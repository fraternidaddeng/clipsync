<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- AppCompat parent: required by AppCompatActivity, which applies the per-app
         locale (偏好·显示·语言, P1#16) to every activity context. Visuals are unchanged —
         the UI is fully Compose-drawn; this theme only sets window chrome. -->
    <style name="Theme.ClipSync" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">#E2E9F2</item>
        <item name="android:statusBarColor">#E2E9F2</item>
    </style>

    <!-- Invisible host for the Quick Settings tile's one-shot clipboard read. -->
    <style name="Theme.ClipSync.Transparent">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
