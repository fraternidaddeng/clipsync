<!--
    Shared charter inputs: the z−1 inset text box and combo box, the toggle switch, the
    fold handle, the filter chip and the stepper button. Moved up from MainWindow so every
    window (and the layout tests) can pick them up by the same keys; they depend only on
    the App-level colour tokens.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 开关：凹槽轨道 + 抬起的钮；开 = 轨道充上流动蓝，物理凸轮微阻尼拨动 -->
    <Style x:Key="CharterToggle" TargetType="ToggleButton">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid Width="40" Height="22">
                        <Border x:Name="Track"
                                Width="40"
                                Height="22"
                                Background="{DynamicResource CsSurfaceInBrush}"
                                BorderBrush="{DynamicResource CsLine2Brush}"
                                BorderThickness="1"
                                CornerRadius="11" />
                        <Border x:Name="TrackFill"
                                Width="40"
                                Height="22"
                                Background="{DynamicResource CsFlowBrush}"
                                BorderBrush="{DynamicResource CsFlowBrush}"
                                BorderThickness="1"
                                CornerRadius="11"
                                Opacity="0" />
                        <Border x:Name="Knob"
                                Width="16"
                                Height="16"
                                Margin="2,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Background="{DynamicResource CsSurfaceUpBrush}"
                                BorderBrush="{DynamicResource CsLineBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform x:Name="KnobScale" ScaleX="1" ScaleY="1" />
                                    <TranslateTransform x:Name="KnobTranslate" X="0" Y="0" />
                                </TransformGroup>
                            </Border.RenderTransform>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- 凸轮死点势能微阻尼与顺滑滑移: X: 0 -> 20 -->
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Knob"
                                                                       Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.X)">
                                            <SplineDoubleKeyFrame KeyTime="0:0:0.24" Value="20" KeySpline="0.16,1 0.3,1" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Knob"
                                                                       Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)">
                                            <DiscreteDoubleKeyFrame KeyTime="0:0:0.0" Value="1.0" />
                                            <SplineDoubleKeyFrame KeyTime="0:0:0.08" Value="1.12" KeySpline="0.2,0 0.4,1" />
                                            <SplineDoubleKeyFrame KeyTime="0:0:0.24" Value="1.0" KeySpline="0.16,1 0.3,1" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Knob"
                                                                       Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)">
                                            <DiscreteDoubleKeyFrame KeyTime="0:0:0.0" Value="1.0" />
                                            <SplineDoubleKeyFrame KeyTime="0:0:0.08" Value="0.94" KeySpline="0.2,0 0.4,1" />
                                            <SplineDoubleKeyFrame KeyTime="0:0:0.24" Value="1.0" KeySpline="0.16,1 0.3,1" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimation Storyboard.TargetName="TrackFill"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1"
                                                         Duration="0:0:0.24" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Knob"
                                                                       Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.X)">
                                            <SplineDoubleKeyFrame KeyTime="0:0:0.22" Value="0" KeySpline="0.16,1 0.3,1" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Knob"
                                                                       Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)">
                                            <DiscreteDoubleKeyFrame KeyTime="0:0:0.0" Value="1.0" />
                                            <SplineDoubleKeyFrame KeyTime="0:0:0.07" Value="1.10" KeySpline="0.2,0 0.4,1" />
                                            <SplineDoubleKeyFrame KeyTime="0:0:0.22" Value="1.0" KeySpline="0.16,1 0.3,1" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Knob"
                                                                       Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)">
                                            <DiscreteDoubleKeyFrame KeyTime="0:0:0.0" Value="1.0" />
                                            <SplineDoubleKeyFrame KeyTime="0:0:0.07" Value="0.95" KeySpline="0.2,0 0.4,1" />
                                            <SplineDoubleKeyFrame KeyTime="0:0:0.22" Value="1.0" KeySpline="0.16,1 0.3,1" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimation Storyboard.TargetName="TrackFill"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0"
                                                         Duration="0:0:0.20" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 折叠区把手（缺省即隐藏）：文字 + 旋转箭头，不是开关——展开一段可选内容，
         不改任何设置。当前唯一使用者是特权直读卡的无线配对段 -->
    <Style x:Key="FoldToggle" TargetType="ToggleButton">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Foreground" Value="{DynamicResource CsText2Brush}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Padding="2,4" Background="Transparent">
                        <StackPanel Orientation="Horizontal">
                            <Path x:Name="Chevron"
                                  Margin="0,1,7,0"
                                  VerticalAlignment="Center"
                                  Data="M0,0 L4,4 L0,8"
                                  RenderTransformOrigin="0.5,0.5"
                                  Stroke="{DynamicResource CsText3Brush}"
                                  StrokeThickness="1.4">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="0" />
                                </Path.RenderTransform>
                            </Path>
                            <ContentPresenter VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Chevron"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                         To="90"
                                                         Duration="0:0:0.22">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Chevron"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                         To="0"
                                                         Duration="0:0:0.18">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseIn" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                            <Setter Property="Foreground" Value="{DynamicResource CsText1Brush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource CsText1Brush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 形态筛选 chip（ADR 0003）：选中 = 流动蓝着色盒（与导航同一词汇），
         未选中留在安静的灰族 -->
    <Style x:Key="FilterChip" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource CsText3Brush}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Margin" Value="0,0,6,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="Bd"
                            Padding="10,4"
                            Background="{DynamicResource CsSurface3Brush}"
                            BorderBrush="{DynamicResource CsLineBrush}"
                            BorderThickness="1"
                            CornerRadius="9">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource CsFlowBrush}" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource CsFlowBgBrush}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource CsFlowLineBrush}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsChecked" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Foreground" Value="{DynamicResource CsText2Brush}" />
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource CsSurfaceBrush}" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 步进器 −/＋ 钮 -->
    <Style x:Key="StepButton" TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource CsText3Brush}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Width" Value="26" />
        <Setter Property="Height" Value="26" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="Transparent">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource CsSurface3Brush}" />
                            <Setter Property="Foreground" Value="{DynamicResource CsText1Brush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 凹陷输入框（z−1：比底更暗）。Padding 横向 12 取自间距阶（tokens §7）；纵向 6 是
         Height=30 减 1px 边框后给 12.5px 文字留出 16px 视口的余量——TextBox 自己把 Padding
         算进内容尺寸，模板里不再重复内边距（Clipboard 布局测试守着不裁字）。 -->
    <Style x:Key="InsetTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource CsSurfaceInBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CsLineBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource CsText1Brush}" />
        <Setter Property="CaretBrush" Value="{DynamicResource CsText1Brush}" />
        <Setter Property="FontSize" Value="12.5" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource CsFlowLineBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 凹陷下拉框（语言选择器，P1#16）：闭合态与凹陷输入框同一 z−1 语汇（同一 Padding），
         弹出面用 z1 卡片底 + 1px 线；19 种语言 chip 放不下，这是偏好页唯一的下拉 -->
    <Style x:Key="InsetComboBox" TargetType="ComboBox">
        <Setter Property="Background" Value="{DynamicResource CsSurfaceInBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CsLineBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource CsText1Brush}" />
        <Setter Property="FontSize" Value="12.5" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="MaxDropDownHeight" Value="320" />
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Foreground" Value="{DynamicResource CsText1Brush}" />
                    <Setter Property="Padding" Value="10,6" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="Bd"
                                        Padding="{TemplateBinding Padding}"
                                        Background="Transparent"
                                        CornerRadius="6">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsHighlighted" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource CsSurface3Brush}" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource CsFlowBgBrush}" />
                                        <Setter Property="Foreground" Value="{DynamicResource CsFlowBrush}" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                  IsHitTestVisible="False" />
                                <Path Grid.Column="1"
                                      Margin="0,1,10,0"
                                      VerticalAlignment="Center"
                                      Data="M0,0 L4,4 L8,0"
                                      Stroke="{DynamicResource CsText3Brush}"
                                      StrokeThickness="1.4" />
                            </Grid>
                        </Border>
                        <ToggleButton ClickMode="Press"
                                      Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="Transparent" />
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <Popup AllowsTransparency="True"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               Placement="Bottom"
                               PopupAnimation="Fade"
                               StaysOpen="False">
                            <Border MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Margin="0,4,0,0"
                                    Padding="4"
                                    Background="{DynamicResource CsSurfaceBrush}"
                                    BorderBrush="{DynamicResource CsLineBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource CsFlowLineBrush}" />
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource CsFlowLineBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
