# ProtocolValidator

This directory is unused. Shared protocol fixtures are validated by
`scripts/validate-protocol.py` (via `scripts/validate-protocol.ps1`).

Install Python dependencies from the repository root:

```
pip install -r requirements.txt
```
