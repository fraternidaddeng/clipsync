# Changelog

本文件记录 ClipSync 的用户可见变更，格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。首个版本 `v0.1.0` 已于 2026-08-26 归档（发布 tag 路径：先 `v0.1.0-rc.1` 验证 `release.yml` 首次实跑，再 `v0.1.0` 正式发布；实际只打到 `v0.1.0-rc.2`，正式 `v0.1.0` tag 未曾推送——已发布的预发布产物即该版本线的终态）。发布叙述与验收声明见 `docs/releases/`（模板：`docs/release-notes-template.md`）。

## [Unreleased]

## [0.4.0] - 2026-09-16

### 动效与微交互升级（重点）

- [双端] **交互动效与质感全量升级（丝绸重力垂坠与空间连续性）**：
  - 彻底解决并摒弃列表增删时的「高度由 0 挤出 / 抽缩坍塌」反模式，改用稳定视口与 FLIP 空间位移让位（`Modifier.animateItem`）。
  - **交付交互式动效预览实验室**（`docs/design/animation_preview.html`），提供 12 组物理动效实机模拟，内置 A/B 质感切换（软羽入舱 vs 生硬挤压，槽位腾挪 vs 原地压缩），支持 1.0x / 0.4x / 1.5x 速率与 GSAP 物理阻尼参数实时调校。
- [Windows] **核心控件微交互重构**：
  - `CharterToggle`：摒弃瞬移机制，引入莱卡相机凸轮过中点机制（`TranslateTransform.X: 0 → 20`、第 70ms 预冲微压 `Scale 1.12 / 0.94`、`SplineDoubleKeyFrame 0.16,1 0.3,1` 双层轨道底色渐变）。
  - `FoldToggle`：Chevron 箭头引入 220ms `CubicEase` 故事板平滑回旋（0° ↔ 90°）。
  - `TrayFlyoutWindow`：托盘小窗实现 8px 气压升降进出舱与卡片触觉微压反馈（`Scale 0.992`）。
  - `PairingApprovalWindow`：补齐审批通过后的 18° 倾斜镜面流光扫过礼赞仪式，对齐移动端。
- [Android] **Compose 交互平滑度优化**：
  - `HomeScreen`：分类筛选药丸引入 `animateColorAsState` 与 `CharterMotion.spec(CharterMotion.DUR_QUICK_MS)`，选中有温润平滑过渡。
  - `PreferencesScreen`：语言与分段选择药丸引入 `animateColorAsState` 与 `CharterMotion.spec` 缓动过渡。

本节全部改动亦包含自 2026-09-01 起的真机测试处置：防火墙未放行导致配对静默失败、退出时点托盘图标崩溃、UI 负向文案过多、输入框字符纵向裁剪、TUN 模式影响同步、语言下拉框闭合态显示代码格式。

2026-09-03 第二批：五个并行工作流在独立分支完成后合入——Windows 配对信标与诊断跟进（`4362f5c`）、Windows 开关实际生效状态与技术债（`a1e8f81`…`7564fde`）、Android 配对期间监听信标（`bf587d0`）、Android 读取路线回升与实时状态行（`246aab5`…`92a1416`）、双端非 UI 层性能（`ff9c94e`、`e750c6b`，审计记录 `docs/performance-audit.md` 第二轮）——再加一轮跨工作流接线与合并后的双端全量验证。

### 新增

- [Windows] **防火墙状态检测与一键放行 / 移除**（真机报告问题 1 主因；设计记录 `docs/adr/0006-firewall-rule-management.md`，项目第一个写系统级配置的功能）。核实：手机扫码后「请在电脑上批准」约 90 秒后报「配对失败」，电脑从未弹出批准窗口——Windows 防火墙没有 TCP 47654 的入站放行规则，配对确认请求被静默丢弃；非管理员会话、之前点过「取消」（Windows 建 Block 规则后不再弹）、网络被归为「公用」而规则只放专用，都表现为「什么都没发生」。新增 `ClipSync.App/Firewall/`：`FirewallInspector` 经 COM `HNetCfg.FwPolicy2` **只读**检测（`dynamic` 晚绑定、不引 COMReference、普通用户权限、线程池执行；任何 COM/绑定异常折成「无法判断」并只记异常类型名），`FirewallVerdictEvaluator` 纯函数给出三态——**已放行**（启用的入站 Allow 规则覆盖当前监听端口且配置文件覆盖当前活动网络类型，或该配置文件防火墙关闭 / 默认放行）、**未发现放行规则**（含规则存在但被禁用 / 配置文件不匹配 / 监听端口 ≠ 规则端口）、**无法判断**（第三方防火墙产品 `HNetCfg.FwProducts` 非零、组策略接管、COM 不可用）；同时列出针对 `ClipSync.App.exe` 的 Block 规则名。`FirewallRuleCommand` 生成固定规则名 `ClipSync TCP 47654` 的 `netsh advfirewall firewall add/delete rule` 命令（另给等价 `New-NetFirewallRule` 供偏好 PowerShell 的人复制），`FirewallRuleElevator` 以 `runas` 动词启动 Windows 自带 `netsh.exe` 触发系统 UAC，Win32 1223（用户在 UAC 上拒绝）如实报「已取消：未获得管理员授权」而非成功或错误；`FirewallRuleWindow` 确认窗先原样展示将执行的完整命令，配置文件**默认仅专用网络**，「同时放行公用网络」复选框就地写明后果（同网段设备可连到该端口，读内容仍需配对）。通路页：网络段新增「防火墙」状态行（未发现规则用赭色「需要你操作」、已放行用通行色、无法判断保持 detail 灰）+「重新检测」，细节行陈述端口不匹配 / Block 规则 / 「规则存在只说明防火墙不拦；路由器 AP 隔离、VPN/TUN 仍可能阻断」；连接卡新增「放行 TCP 47654 入站」一行——只读 Mono 命令框 + 「复制命令」（走与历史复制相同的回声抑制路径）+ 「放行（需要管理员）」+ 「移除规则（需要管理员）」（只在检测到本应用命名的规则时出现），当前网络为公用时显示赭色提示盒；每次放行 / 移除后重新检测，结果行陈述防火墙此刻的实际状态。**规则已存在时「放行」按钮禁用**（`CanExecute` 随 `FirewallManagedRuleExists` / `FirewallBusy` 联动），避免 netsh 在同名下重复建规则。配对二维码窗口在检测到未放行时显示提示盒（`Pairing_FirewallHint`，绑定主视图模型 `FirewallRuleMissing`），首次引导的配对页加一句防火墙提示（`Onboarding_Pair_Firewall`）。App 层在监听端口就绪后与每次网络变化恢复后各做一次只读检测，不阻塞启动。31 新键 × 19 语（`check-i18n-parity.py` 照绿）。测试：`FirewallVerdictEvaluatorTests`（三态判定、配置文件匹配、端口不匹配、Block 规则、第三方接管、组策略）、`FirewallRuleCommandTests`（netsh 逐 token、PowerShell 等价、规则名）、`MainViewModelFirewallTests`（三态文案、取消确认窗零执行、UAC 拒绝报取消、netsh 退出码、移除只对本应用规则、规则已存在禁用放行直至移除）。
- [Windows] **配对链路进入托盘诊断日志 + 批准窗口不再容易错过**（真机报告问题 1 的另一半：手机端「约 90 秒」与 `PairingService.ApprovalTimeout` = 90 s 吻合，说明也可能是请求已到达而批准窗口没被看到）。新增 `DiagnosticsLoggerFactory` 把 `ClipSync.Peer` 的 `ILogger` 事件接入托盘诊断日志（`PeerSyncHost` / `PeerServer` / `PairingService` 新增 `ILoggerFactory` 注入点，测试可留空），`DiagnosticsLogCodes` 只把事件名映射为 `peer_` 前缀的 snake_case 状态码——状态值仅在属于代码中声明的封闭集合时才跟在码后（配对错误码 `PairingErrorCodes.All`、会话 / 帧 / 对端错误码、限流种类、存储冲突阶段、后台循环名、监听端口），格式化消息、设备 ID、显示名、地址、自由文本一律不入码（集合外的值读作 `_unknown`），非 `ClipSync.*` 类别（Kestrel/ASP.NET）不记录；`PeerLog` 新增 `PairingConfirmReceived`（EventId 21，请求抵达进程即记录、先于正文解析），`PeerServer` 超长 / 不可解析正文与 `PairingService` 自配对拒绝三处补 `PairingConfirmFailed(SchemaViolation)`——扫码后「配对失败」的第一问「请求到底到没到电脑」从此有据可查（21 个 PeerLog 事件全部可见）。`WpfPairingApprover` 在批准窗口 `Show()` 后调用 `FlashWindowEx`（`FLASHW_ALL | FLASHW_TIMERNOFG`：任务栏按钮与标题闪烁直到窗口进入前台——`Activate()` 在其他进程持有前台时会被 Windows 前台锁拒绝，恰是用户盯着手机的情形），并经两个可选回调让托盘发气泡：请求出现时「「X」请求配对 / 请在弹出的批准窗口中确认或拒绝。窗口在任务栏闪烁。」（名字截到 40 字符、不拆代理对、不带 ID 与平台），90 秒未批准时「配对请求已超时 / 手机等待约 90 秒未获批准。请在手机上重新扫描新的二维码。」（4 新键 × 19 语）。测试：`DiagnosticsLogCodesTests`（允许列表、`_unknown`、类别过滤、异常只记类型名、snake_case）、`WpfPairingApproverTests`（名字截断）、`PairingHttpTests` 新例（畸形正文仍留下 `pairing confirm request received` 与 `failed code=PAIRING_SCHEMA_VIOLATION` 证据）。
- [Android] **配对等待页分阶段、不可达失败分层提示、429 单独映射**（真机报告问题 1 的手机侧：原来扫码后只有一句「等待批准…」，连不上电脑与电脑没人批准看起来一模一样）。`PairingConfirmApi.confirm` 新增 `onPhase` 回调，`PairingConfirmClient` 经 OkHttp `EventListener.connectEnd` 在 TCP/TLS 建立、请求上线的那一刻报 `PairingPhase.AWAITING_APPROVAL`（每次尝试只报一次；证书 pin 不符永不报），此前为 `CONNECTING`；`PairingUiState.Submitting` 带 `hostCount` / `phase` / `elapsedSeconds`（1 秒 ticker 作为提交协程的子任务，结束即停），等待页据此显示「正在连接电脑… / 正在尝试二维码里的 N 个地址，每个最多等 6 秒。」或「等待批准… / 请在「X」的剪剪相传窗口中批准此手机。」，下方「已等待 N 秒」。不可达失败按 errno 分类：新增 `UnreachableReason`（REFUSED / TIMEOUT / NO_ROUTE / HOST_UNREACHABLE / UNKNOWN），`UnreachableReasons.fromErrno` 为纯函数（`ConnectErrnos` 由 `AndroidConnectFailures` 从 `OsConstants` 的 ECONNREFUSED / ETIMEDOUT / ENETUNREACH / EHOSTUNREACH 惰性读入——只在异常链里真找到 `ErrnoException` 后才碰 Android 类，纯 JVM 测试不触桩），无 errno 时按异常类型回退；`PairingConfirmOutcome.Unreachable` 改为按二维码顺序保留每个主机的 `HostAttempt(host, reason)`，`UnreachableReasons.primary` 取主因（拒绝 > 超时 > 主机不可达 > 无路由——拒绝说明地址对、没人监听，超时指向用户能修的防火墙）。新增 `LocalNetworkSource` / `ConnectivityLocalNetwork` 读手机当前 IPv4 与前缀长度（只在一次不可达失败之后咨询，绝不用来挑选或探测主机；读不到即「未知」不猜），`anyHostOnSameSubnet` 判断二维码里是否有地址与手机同网段；`UnreachableHint` 据「主因 × 是否同网段」给出第二段提示：超时且同网段 →「最常见原因是电脑的 Windows 防火墙没有放行 TCP {port} 入站，或路由器开启了 AP 隔离。放行方法见电脑端「通路」页或安装指南。」，超时且不同网段 → 确认同一 Wi-Fi 或在电脑端「额外监听地址」填手机能访问的地址，超时且网段未知 → 检查防火墙，拒绝 → 电脑上没有程序在监听该端口，无路由 / 主机不可达 → 电脑可能已休眠、IP 已变、或二维码里排在前面的是虚拟网卡地址；UNKNOWN 不加第二段。HTTP 429 单独映射：`Denied(RATE_LIMITED)`（正文不可解析时也如此，不再落 `ProtocolViolation`），新失败类型 `PairingFailure.RATE_LIMITED` →「电脑暂时拒绝了过多的配对请求。请等一分钟后重新出示二维码再试。」。`reset()` 现会取消在途提交。`pairing_fail_unreachable` 改「请确认两台设备在同一 Wi-Fi」，通路页 `network_unreachable_fact` 补「且电脑防火墙已放行 TCP 47654 入站」。9 新键（含 2 个 plurals）+ 2 改写 × 19 语。测试：`UnreachableReasonsTest`、`LocalNetworkTest`（IPv4 解析、/24 与边界前缀、主机名与 IPv6 永不匹配）、`UnreachableHintTest`、`PairingConfirmClientTest`（分类注入、429 两态、`attempts` 顺序）、`PairingViewModelTest`（阶段推进与秒数、不可达明细、同网段判定、RATE_LIMITED 映射、reset 取消）。
- [双端] **「使用前必读：隐私与风险说明」文档与应用内入口**（真机报告问题 3 的折中执行：UI 只保留不可逆后果与决策前必知项，零散的风险认知归入一份用户向文档）。新增 `docs/privacy-and-risks.md`（en）/ `.zh-CN.md` / `.ja.md`，13 节：内容去哪里、已配对设备是完全信任的、到达对端后剪贴板是共享的（本反馈指出的遗漏项）、敏感内容也会同步、图片与元数据、局域网上其他设备能看到什么、防火墙放行意味着什么、Android 特权直读（adb / 无线调试）、蓝牙备援、日志与诊断、数据文件与备份、隐私默认值一览、延伸阅读。入口：Windows 首次引导第 1 页「先说清楚」块下方链接式按钮（`Onboarding_PrivacyDoc_Link`）与偏好 · 帮助新行「隐私与风险说明 / 打开」（`Prefs_PrivacyDoc_Title/Desc/Open`），`Docs/PrivacyDocLink` 按界面文化选版本（任意 `zh-*` → zh-CN，`ja` → ja，其余 → en），`Docs/ExternalLinks` 只在点击时用系统浏览器打开 GitHub 仓库 `blob/main/docs/` 下的文档，打不开时行内显示「没有可用的浏览器。」（`Open_Url_Failed`）；Android 引导欢迎页「先说清楚」下方 `ExternalLink`（流动蓝、带 ↗、点击前不预取）与偏好 · 帮助 `ActionRow`（`prefs_privacy_doc_title/desc`），`docs/PrivacyDoc` 同一选版规则，`ui/ExternalLinks.rememberPrivacyDocOpener` 经 `ACTION_VIEW` 打开、无浏览器时 Toast `open_url_failed`。README 三语「隐私与安全」节首行加链接。5 + 4 新键 × 19 语；测试 `PrivacyDocLinkTests`（Windows）/ `PrivacyDocTest`（Android）钉死文化 → 文档映射与 URL 形态，`OnboardingContentTest` 补链接键。
- 【第二批】[Windows] **二维码窗口期间的 2 秒配对信标 + 多网卡地址按梯队排序**（真机报告问题 1 的第三块：手机端此前只能凭二维码里的地址顺序逐个试连，虚拟网卡地址排在前面时每个都要等满超时）。`DiscoveryBeaconSchedule` 把既有的 UDP 47653 发现广播分成 5 分钟常态 / 2 秒配对两档（引用计数），`PeerSyncHost.BeginPairingBeacon()` 返回 `IDisposable`：开启即广播一次并把 Timer 改到 2 秒，恢复流程重建广播器后节奏不变；`PairingQrWindow` 出码时 Begin、无可达地址 / 配对完成 / 关窗时 End；诊断码 `pairing_beacon_started` / `pairing_beacon_stopped`。**载荷不变**（设备 ID / 端口 / 证书指纹），仍只发不收。`ReachableHostOrdering.Order` 把公布给二维码的地址按梯队排序：Up 的 Wi-Fi / 以太网且有 IPv4 网关 → 用户「额外监听地址」→ 其他有网关 → 无网关物理网卡 → 虚拟网卡（vEthernet / Hyper-V / WSL / VMware / VirtualBox / Host-Only / TAP / Wintun / WireGuard / Tailscale / ZeroTier / Docker / Npcap / Loopback），梯队内稳定；绑定集合不变，只改公布顺序，仍取前 8。测试：`DiscoveryBeaconScheduleTests`、`PeerSyncHostPairingBeaconTests`、`ReachableHostOrderingTests`。
- 【第二批】[Windows] **托盘区分「批准超时」与「对端中止」**：`PairingCandidate` 附带 `ApprovalTimeout` 令牌，`WpfPairingApprover` 在取消回调里据此分类——超时走 `onRequestTimedOut`（`Tray_PairingTimeout_BodyFormat`，`{0}` 秒取 `PairingServiceOptions.DefaultApprovalTimeout`），手机先挂断或主机关闭走新增的 `onRequestAborted`（`TrayIconController.ShowPairingAbortedNotice`，`Tray_PairingAborted_Title/Body`「配对请求已中止 / 手机在批准前断开了连接。若不是你主动取消，请重新出示二维码再扫。」）。本批集成时补上 `App.xaml.cs` 里 `onRequestAborted` 的实际接线（W2 只实现了回调与气泡，未在构造处传入）。测试 `WpfPairingApproverTests`（`Classify` 三态、`NoticeFor` 选路）。
- 【第二批】[Windows] **诊断码按类别前缀 + 蓝牙宿主接入诊断**：`DiagnosticsLogCodes` 按 `ILogger` 类别选前缀——`ClipSync.Peer.Bluetooth*` → `bt_`、`ClipSync.Peer*` → `peer_`、其余 `ClipSync.*` → `app_`；`BluetoothSyncHost` 构造处传入 `DiagnosticsLoggerFactory.Instance`，`BluetoothLog` 的 HandshakeRefused 只放行 `Bt1ErrorCodes.WireCodes`、SessionEnded 放行协议码 + `SessionEndOutcomes`（clean / cancelled），设备 ID / MAC / reason / 异常种类一律不入码；删除 `PeerSyncHost` 与 `PeerLog.ServerListening` 重复的 `peer_server_started_port_` 写入。`DiagnosticsLogCodesTests` 随之扩展。
- 【第二批】[Windows] **开关旁的「实际生效状态」事实行**（任务「开关的状态很多都实际没有显示」）。状态映射抽为纯函数 `SettingStatusMapper`（`ViewModels/SettingStatus.cs`，三种色调：安静灰 / 流动蓝 / 赭色「开了但系统没照做」），`MainViewModel.SettingStatus.cs` 持有各行状态并在输入变化时重算。覆盖的开关：**开机自启**（`StartupRegistration.Probe()` 读回 HKCU Run 与 StartupApproved——已登记 / 未能登记 / 被任务管理器停用 / 关了但注册表仍在 / 无法读取；`Status_Startup_*`）、**额外监听地址 / 语言**（只在保存值与本进程启动值不同时变赭「已保存 · 重启后生效」，`Status_RestartPending`）、**暂停捕获 / 私密模式**（「已暂停 · 本次已跳过 {0} 条」，`Status_Paused_SkippedFormat` / `Status_Private_SkippedFormat`，跨一次暂停/私密区间计数）、**自动写入（文本）**（门控优先，再看最近一次真实写入：尚未收到 / 已写入 / 未成功，`Status_AutoApply_*`，与 `/v1/peer/health` 上报的同一份证据）、**图片同步**（关：本机只收发文本；开：等待手机连接 / N 台已连接，`Status_ImageSync_*`）、**蓝牙备援**（沿用监听器文案，适配器不可用变赭）、**屏蔽进程**（编辑中未保存 / 已生效 N 个 / 未屏蔽，`Status_BlockedProcesses_*`）；托盘浮窗页脚补一行赭色事实（防火墙未放行 `Flyout_Detail_FirewallMissing` / 蓝牙适配器不可用）。20 新键 × 19 语。测试：`SettingStatusMapperTests`、`MainViewModelSettingStatusTests`（可注入 `startupRegistrationProbe`）。
- 【第二批】[Windows] **本批集成接线（跨工作流）**：① 所有 `CaptureResult.Rejected` 分支都调用 `NoteCaptureRejected(reason)`（此前只有 TooLarge 调），「本次已跳过 N 条」计数因此真实累加；② 启动时 `ReconcileLaunchAtStartup` 之后立刻 `RefreshStartupStatus()`，事实行反映这一次登记的结果而非 `InitializeAsync` 早一步读到的旧值；③ **图片同步对端状态**：`SyncSessionEngine.ProtocolVersion` 暴露会话协商到的线版本，`PeerServer.ImageCapableDeviceCount` / `PeerSyncHost.ImageCapableDeviceCount` 统计「已就绪且走 /v2」的去重设备数（判据：手机只在自身图片同步开时才拨 /v2，电脑只在自身开时才接受 /v2，故 v2 会话 ⇔ 双方都开），`MainViewModel.UpdatePeerStatus` 新增可选参数 `imageCapableCount` 写入 `ImageCapableDeviceCount`，`SettingStatusMapper.ImageSync` 新分支：已开、有连接、但 0 台协商到图片 → 「已开 · 已连接的手机未开启图片同步，本次会话只同步文本」（`Status_ImageSync_PeerTextOnly`）；未上报（null）时不对手机做任何断言；④ **自动写入（图片）最近结果**：远端图片写入本机剪贴板处（`adapter.WriteImage`，任何原生失败都抛异常）记录 `RecordRemoteImageApplyOutcome(ok)`，`AutoApplyImagesStatus` 显示「最近一次收到的图片已写入本机剪贴板」（流动蓝）/「最近一次图片写入剪贴板未成功，图片留在历史里」（赭），未尝试过时不显示（`Status_AutoApplyImages_Applied/Failed`）。3 新键 × 19 语。测试：`SettingStatusMapperTests`（图片行三态、PeerTextOnly 分支与 null 不断言）、`MainViewModelSettingStatusTests`（图片证据独立于文本行、`ImageCapableDeviceCount` 随在线/离线清零）、`PeerSyncIntegrationTests.ImageCapableDeviceCountFollowsTheNegotiatedWireVersion`（同一设备 v1 + v2 两条会话：连接数 1、图片能力数 1，v2 会话关闭后回到 0）。
- 【第二批】[Android] **配对页期间监听电脑的局域网信标 + 等待页可取消**（与 Windows 信标配套）。新增 `pairing/DiscoveryBeacon.kt`（纯 JVM：信标解析、按 `device_id` + `cert_sha256` 与二维码匹配、`BeaconStatus`、`hostsPreferringBeacon` 地址重排）与 `platform/UdpDiscoveryListener.kt`（绑定 UDP 47653 + `MulticastLock`「clipsync-pairing」，绑定失败即 UNAVAILABLE，任何异常吞成不可用）；Manifest 新增普通权限 `CHANGE_WIFI_MULTICAST_STATE`。`PairingViewModel` 只在 Review / Submitting / Failed 且配对页可见（Lifecycle ON_START/ON_STOP）时监听，Idle / Paired / `onCleared` 即停止——**MulticastLock 仅配对页期间持有**；提交前把信标源 IP 移到 hosts 最前（不在列表则追加到最前），`hostCount` 随之更新；等待页显示「正在监听电脑的局域网信号…」→「已在当前网络上收到「X」的信号」（`pairing_beacon_listening` / `pairing_beacon_seen`）。`UnreachableHint` 新增 TIMEOUT × 已收到信标分支，优先于同网段判定：「手机已收到这台电脑在当前网络上的信号，但连接超时——几乎可以确定是电脑的 Windows 防火墙没有放行 TCP {port} 入站」（`pairing_fail_unreachable_hint_timeout_beacon`）。等待页新增「取消」（`pairing_cancel`）：`cancelSubmission` 取消在途 Job，`PairingConfirmClient` 改为 `enqueue` + `suspendCancellableCoroutine` 让协程取消真正 `call.cancel()`；回到 Idle 并显示一次性 `pairing_cancelled_notice`「这枚二维码已被使用，请在电脑上重新出示后再扫」。7 新键 × 19 语。测试：`DiscoveryBeaconTest`、`PairingViewModelTest`（各状态转换的 start/stop、取消、信标重排）、`UnreachableHintTest`（优先级）。
- 【第二批】[Android] **读取路线降级后可回升**：`ClipboardAccessCoordinator.tryRecover` 向上重探——首选路线探测 READY 即按原 `switchTo` 顺序切回（先停旧、再刷基线、再启新，同一时刻只有一个 backend）；访问状态记录 `shortfall`（原因 / 来源路线 / 错误码 / 时刻）与最近回升时刻并以 `StateFlow` 外露；会话改按门控原因（暂停同步 / 私密 / 暂停捕获）判定并外露实时状态；Activity 回到前台时顺带重探；服务每 10 个健康 tick（5 分钟）在降级态重探一次；新增捕获结果内存计数 `CaptureTally`。通路页路线卡新增「重新探测首选路线」（`RouteActionId.RECOVER_PREFERRED`，经会话锁回升并在结果条陈述结果；测试后台读取通过后也立即回升）。单测覆盖降级 → 回升、首选仍 FAILED 留在原地、不重复捕获。
- 【第二批】[Android] **通路 / 偏好页的实时状态事实行**：通路 · 本机读取改以协调器实时状态为准（纯函数 `readRouteStatus`：「当前读取路线：X · 首选 Y 已于 HH:mm 因通道故障降级（码）」/「首选 Y 自 HH:mm 起未就绪」/「后台读取已停止：「暂停自动捕获」开启中」，降级态 52% 半填充、门控态灰面「已暂停」），路线卡标注「当前正在使用此路线」，首选卡未在运行时给赭 / 流动色事实行；本机服务段补电池优化放行事实与「开关已开但服务未运行」事实；偏好页开关下方新增事实行（数据源 `preferencesRuntimeFacts`：服务流 + 协调器 + 会话 + `CaptureTally`；纯函数 `preferencesStatusLines`）——后台同步服务运行 / 连接 / 被系统回收（赭）、暂停同步 / 私密 / 暂停捕获生效中、当前路线与本次捕获计数、跳过敏感条数、关闭图片同步后未同步张数、系统关闭通知时赭色提示；引导第 4 页悬浮窗 / 电池权限改按向导步骤实测结果标注已开启。28 新键 + 2 复数键 × 19 语。测试：`ReadRouteStatusTest`、`HealthViewModelLiveRouteTest`、`PreferencesStatusTest`、`CapabilityRoutesTest` 扩展。
- 【第二批】[Android] **本批集成接线（跨工作流）**：① **最近同步时间**：`SyncEngine` 新增 `onContentSynced` 回调，只在内容真正过线时触发（远端批次本地提交、或对端 ack 了本机发出的序列；握手 / 向量 / 心跳 / 无正文终止标记不算），`SyncSupervisor.lastSyncAtMs`（`StateFlow<Long?>`）→ `ClipboardSyncService.lastSyncAtMs`（只前进，不被新监督器的初始 null 抹掉）→ `SyncHealth.lastSyncAtMs` → 通路页网络段设备行第三行「最近同步 HH:mm」（`network_last_sync_format`，`ConduitDeviceUi.lastSyncLine`；无记录时不显示，会话掉线后仍保留——它记的是内容何时过线，不是连接标志）。② **蓝牙权限被拒**：`PreferencesRuntimeFacts.bluetoothPermissionGranted`（`BluetoothSyncConnector.hasConnectPermission`：API 31+ 查 `BLUETOOTH_CONNECT`，以下恒 true；本应用不申请 `BLUETOOTH_SCAN`，只连已配对设备），`preferencesRuntimeFacts` 新增 `refreshTicks`，`MainActivity.onResume` 每次回到前台 bump 一次让权限类事实重采样；蓝牙备援开关行（通路页 `BluetoothFallbackCard`）在开关开且权限被拒时显示赭色「蓝牙权限未授予 · 备援不可用，去系统设置授权」（`network_bt_permission_denied`，点击打开本应用的系统权限页）。③ **自动写入结果**：`InboxDelivery.lastApplyOutcomes`（`InboxApplyOutcome`：applied / isImage / errorCode / 时刻）只记录真实尝试——被门控跳过的投递不留记录，图片 blob 或 MIME 缺失记 `IMAGE_UNAVAILABLE`；偏好页「自动写入剪贴板」与「图片自动写入」各显示本类最近一次结果：「已开 · 最近一次收到的内容已写入剪贴板」（流动蓝，`prefs_live_auto_apply_applied`）/「已开 · 最近一次写入剪贴板失败（错误码），内容留在历史里」（赭，`prefs_live_auto_apply_failed_format`）；关闭或暂停同步时不显示。④ **图片同步是否被对端接受**：`SyncConnectionState.Connected` 新增 `protocolVersion`（IP 会话 1 或 2、蓝牙恒 1），`PreferencesRuntimeFacts.ipSessionProtocolVersion` 据此在图片同步开、且 IP 会话只协商到 v1 时显示安静灰「已开 · 电脑端未开启图片同步，本次会话只同步文本」（`prefs_live_image_sync_peer_text_only`；对端的设置不是本机能修的，故不用赭色）。5 新键 × 19 语。测试：`SyncEngineTest`（`onContentSynced` 在提交与 ack 各计一次、握手 / 终止标记不计）、`SyncSupervisorTest`（`Connected` 断言携带协商版本，重启后 1 → 2）、`SyncHealthFlowTest`、`CapabilityRoutesTest`（设备行「最近同步 14:32」、掉线仍保留）、`PreferencesStatusTest`（自动写入两行按类别独立、图片同步 v1/v2/null 三态、蓝牙权限四组合）、`InboxDeliveryTest`（只记真实尝试、错误码、图片缺失码）。

### 变更

- [Windows] **三条 adb 相关文案由否认句改为正向行为描述**（真机报告问题 3；量化后真正自我辩解式的文案双端共 5 条，其中 3 条 adb 相关本轮改写）：`Conduit_Privileged_Desc` 结尾「全程需你明确同意，绝不静默。」→「每次启动都由你在这里点击触发。」；`Conduit_Privileged_ConsentBody` 中「adb 只在你每次点击时运行，不会常驻或静默执行。」→「adb 只在你点击「启动特权直读」时运行一次，向手机发送一条启动命令；其他时候不调用 adb。」；`Onboarding_Privileged_Consent` 中「绝不常驻或静默执行，每一步都需要你的明确同意。」→「向手机发送一条启动命令；其他时候不调用 adb。」。19 语同步改写。「做不到就直说」（`Onboarding_Honesty_Body` / `onboarding_honesty_body`）作为刻意的品牌声音**原样保留**；实质承诺（正文不进日志、撤销不远程删除）保留。README 三语与 `docs/install.md` 的同款句子（「应用**绝不**静默调用 adb」）同步改为正向。措辞规范写入设计纲领 §5.9（见「文档」）。
- [Windows] 设备重命名输入框字体由 Mono 改 Sans（真机报告问题 4 附带；宪章：Mono 缺中文、度量高，只保留给 IP / 端口 / 密钥 / 命令类机器文本）。
- 【第二批】[Windows] **通用输入样式上移 App 级资源字典 + 布局测试**：凹陷输入框 / 下拉框（`InsetTextBox` / `InsetComboBox`）、开关、折叠把手、形态 chip、步进钮六个样式从 `MainWindow.xaml` 迁到 `Resources/CharterInputs.xaml`（键名不变，`App.xaml` 合并），`MainWindow.xaml` 减 287 行；`InsetTextBox` / `InsetComboBox` 横向 Padding 10 → 12 落回间距阶（纲领间距 token）。新增 STA 布局测试 `CharterInputsLayoutTests`：`Application.LoadComponent` 加载字典，Height=30 的框在默认字体与随包 CsSans 12.5 / CsMono 11.5 下 `PART_ContentHost` 视口 ≥ 内容高度（钉住 [Unreleased]「输入框字符纵向裁剪」修复不回退），`InsetComboBox` 闭合态显示 record 的 `DisplayName`。
- 【第二批】[Windows] **`MainViewModel` 拆出 `FirewallViewModel` 与 `UpdateViewModel`**：防火墙检测 / 命令展示 / 放行 / 移除迁入 `FirewallViewModel`（构造参数 `firewallInspector` / `firewallElevator` / `firewallRulePrompt` 转交），版本 / 检查 / 下载迁入 `UpdateViewModel`；XAML 绑定改 `Firewall.*` / `Update.*`；保留 `FirewallRuleMissing`、`RefreshFirewallStatusCommand`、`UpdateReadyToApply`、`UpdateStatus` 四个转发成员供 `PairingQrWindow` 与 `App.xaml.cs` 既有引用；`MainViewModel` 1960 → 1687 行。`MainViewModelFirewallTests` / `UpdateTests` 改走子 VM 并补转发断言。
- 【第二批】[Windows] **`Prefs_Subtitle` 去掉口号位**：「剪贴板正文永不出现在日志里」已在诊断窗情境化出现（纲领 §5.9 一条路径一次），偏好页副标题改为本页事实「改动即生效 · 需要重启才生效的项会在行内标出」，与新增的行内「已保存 · 重启后生效」事实行呼应；19 语同步。
- 【第二批】[Android] **配对失败分类补 `NO_RESPONSE` / `TLS_FAILURE`**：`PairingConfirmClient` 读阶段 `SocketTimeoutException` → `NoResponse(READ_TIMEOUT)`（「已连上电脑，但电脑在限时内没有应答」，`pairing_fail_no_response`），非指纹 `SSLException` → `NoResponse(TLS_FAILURE)`（「与电脑的加密连接未能建立（不是证书指纹问题）」，`pairing_fail_tls`）；`ProtocolViolation`「版本不匹配」只留给真正的协议 / JSON 问题，不再把读超时误报成版本问题。`cancelReview` 并入 `reset`。测试 `PairingConfirmClientTest`（分类、协程取消不等读超时）。
- 【第二批】[Android] **通知承诺按纲领 §5.9 去重**：「通知不含剪贴板内容」收敛到通知渠道组描述与引导第 4 页权限一览两处；三个渠道描述与 `prefs_inbox_notify_desc` 去掉重复的那半句、只保留各自功能事实；其余「不会 / 永不」句均为实质承诺或能力边界，保留不动（`onboarding_honesty_body` 按要求未改）。19 语同步。
- 【第二批】[Android] **detekt / ktlint 基线两次重生成，只收陈旧条目与行号漂移、不收新违例**（沿 `e1d2826` 先例）：W5 合并前 detekt 473 → 464、ktlint 1993 → 1848（删除 pairing 侧在 `b5bbb60` 重排版后失效的条目与已修掉的 `ClipboardAccessCoordinator` 条目）；本批集成后 detekt 464 → 455（删 9 条 W4 改写后已不匹配任何代码的 `ImageCodec` / `ProtocolStrictJson` / `MediaBlobStore` 条目）、ktlint 1848 → 1776（删 72 条同样来源加 `PairingConfirmClientTest`）。逐文件逐规则计数无一增加；本批新增代码触发的 detekt（`ComplexCondition` / `ReturnCount` / `CyclomaticComplexMethod` / `MaxLineLength`）与 ktlint（`chain-method-continuation`）违例全部回到代码里修掉，`syncHealthFlow` / `buildHealthScreenState` 因新增一个参数越过 `LongParameterList` 阈值，按仓库既有做法加 `@Suppress`。
- 【第二批】[双端] **非 UI 层性能第二轮**（`docs/performance-audit.md` 2026-09-03 节；不改线格式、不改用户可见行为、不引新依赖，每处快路径都有以旧实现为 oracle 的等价性测试）。Windows（`ff9c94e`）：base64url 编解码改 `System.Buffers.Text.Base64` + `SearchValues` + `ArrayPool`，规范性用尾位比特判定，`clip_payload_chunk` 校验只算长度、分片只解码一次——256 KiB 分片 `TryDecodeBase64Url` 3.76 ms / 5.16 MB → 0.38 ms / 0.79 MB，接收链路 Parse + `TryDecodeChunk` 11.01 ms / 12.24 MB → 0.79 ms / 1.84 MB，`ImageChunks.Split` 4 MiB 17.7 ms / 44.7 MB → 5.7 ms / 11.7 MB；`ProtocolReader*` 把 UTF-8 缓冲直接交给 `JsonDocument`、`ProtocolWriter` 改 `ArrayBufferWriter`（600 KiB 文本帧 Serialize 4.48 → 3.66 ms）；`ReplayWindow` 直接对 UTF-16 内存做 SHA-256；`WebSocketSyncTransport` 单帧不经 `MemoryStream`；`MediaBlobStore.Commit` 复用流式累积的 SHA-256、`ImageThumbnail.DecodeBounded` 只读头部（UI 线程不再对整个 blob 哈希）。Android（`e750c6b`）：`ProtocolJson.validatePayloadChunk` 改 `Base64Url.isAlphabet / decodedLength`（正则 + 丢弃式解码 → 字母表 + 算术长度），`ProtocolStrictJson.scan` 逐字符 UTF-8 计数、值就地校验、孤立代理项流式状态机——256 KiB 分片帧 `SyncWire.decode` 6.02 → 1.93 ms、接收链路每片 5.95 → 2.17 ms；`MediaBlobStore.commit` 复用流式哈希（4 MiB begin/append/commit 10.98 → 6.38 ms）；`ByteArray.toLowerHex` 查表。存储 10k / 50k 行基线全部测过、索引均命中，本轮不改。

### 修复

- 【真机 09-16】[双端] **CI 红灯（自 `50bc1cc` 起，与无线配对提交无关）**：`AndroidConnectFailures.classify` 在 JVM 单测里调用未桩的 `android.util.Log`（三条 `PairingConfirmClientTest` 崩）且 `ArrayList(4)` 触发 detekt MagicNumber；Windows `CharterInputsLayoutTests` 用系统字体断言行高塞进 16px 视口，GitHub-hosted runner 默认字体量出 16.63px；`50bc1cc` 通路条改写后 ktlint 才轮到跑——`RouteErrorHints` 函数体换行、`ConduitStatusBand` 新赋值未按 multiline wrapping，且 `HealthScreen` 基线行号漂移。`Log.i` 改为失败可吞、容量改 `mutableListOf`，系统字体例只钉单次 Padding 与约 16px 视口（不拿系统行高和视口比大小），裁切仍由随包字体例负责；新增代码回代码里修，基线只改 `HealthScreen` 漂移行号、条目未增。
- 【真机 09-16】[Windows] **特权直读的无线配对在本机 Debug 与 adb 37 下列表形态下再次可用**：两处叠在一起。其一，`AdbLocator` 只找随包 platform-tools、`ANDROID_*`、`%LOCALAPPDATA%\Android\Sdk` 和 `PATH`——本机 Debug 既不随包、PATH 上也没有 adb，仓库里的 `.tools/android-sdk` 与 `android/local.properties` 的 `sdk.dir` 从未被看见；卡片「检测手机」说找不到 adb，点「出示二维码」却被 `CheckMdnsSupport` 的「adb 不可用 → false」折成「此 adb 不支持 mDNS」。现沿 `BaseDirectory` 向上最多 8 层找 `.tools/android-sdk` 与 `local.properties`，二维码入口在没找到 adb 时改说「未找到 adb」。其二，platform-tools 37 在 `adb connect IP:端口` 成功后把设备列成 `adb-…._adb-tls-connect._tcp` 而不是刚才拨的 `IP:端口`；会话对账只认 host:port，下一轮探测把刚连上的会话判成「已消失」，卡片立刻把「已连接」改成「无线连接已断开」。现把 mDNS 服务名 serial 认作无线会话，列表里有就绪的 connect 名即视为同一条活会话。单测：`AdbLocatorTests`、`WirelessSessionDiagnosisTests`、`WirelessAdbAssistantTests`、`MainViewModelWirelessTests`、`AdbDeviceListParserTests`。
- [Windows] **退出时点托盘图标不再崩溃**（真机报告问题 2，结论成立）。根因：托盘「退出」触发 `Application.Shutdown()` 后到 `OnExit` 返回之间有数秒窗口，托盘图标仍可点击，此时任何 `Window.Show()` 抛 `InvalidOperationException`；而托盘鼠标事件直接来自图标的原生 WndProc，在 WPF dispatcher 的异常处理之外，`Dispatcher.HasShutdownStarted`（`OnExit` 之后才翻转）与 `DispatcherUnhandledException` 都覆盖不到——异常直接杀进程。修复：`App` 自维护 `isExiting` 标志，托盘「退出」统一走 `RequestExit()`（先记意图再 `Shutdown()`）；纯函数 `TrayFlyoutGate.CanShowFlyout(isExiting, dispatcherShutdownStarted)` 把守托盘单击 / 呼出快捷键的浮窗入口；`TrayFlyoutWindow.ShowFlyout()`、`TrayIconController` 的托盘单击回调 / 主窗 `Show()` / 诊断窗 `Show()`、`WpfPairingApprover` 的批准窗 `Show()` 全部本地 catch `InvalidOperationException` 并写诊断码（`flyout_show_refused` / `tray_flyout_failed` / `tray_open_refused` / `diagnostics_show_refused` / `pairing_approval_show_refused`；批准窗打不开时如实回「未批准」，不悬挂）。另加三道全局兜底：`DispatcherUnhandledException`（运行中 → 记码后 `RequestExit()` 优雅退出而非留下半残托盘进程；已在退出中 → 吞掉，让干净退出不以崩溃转储收尾）、`AppDomain.UnhandledException`（运行时无论如何会终止，只留证据）、`TaskScheduler.UnobservedTaskException`（记码并 `SetObserved`）；`UnhandledExceptionCodes` 只记异常**类型名**（`unhandled_ui_*` / `unhandled_fatal_*` / `unobserved_task_*`，聚合异常取最内层），消息与栈永不入日志（可能含剪贴板正文）。三个 sink 在 `OnStartup` 第一个 await 之前注册。测试：`TrayFlyoutGateTests`（四组合）、`UnhandledExceptionCodesTests`。`docs/manual-qa-checklist.md` 第 5 节新增「退出过程中连点托盘图标 / 快捷键 / 诊断，进程不崩溃」。
- [Windows] **输入框字符纵向裁剪**（真机报告问题 4：8 个输入框中文与带下伸部字母被裁）。根因修正：不是行盒 / 字号账，而是 `InsetTextBox` 模板里 `PART_ContentHost` 的 `Margin="{TemplateBinding Padding}"` 与 WPF 对 `ScrollViewer` 内容宿主自动转发的 Padding **双重生效**——实际文字视口只剩约 4px。修法即删去这一行 Margin；原建议的「Height 换 MinHeight / `LineStackingStrategy` 锁行盒」不再需要，不引入新的行盒规则。
- [Windows] **语言下拉框闭合态不再显示 record 的 `ToString()`**（真机报告问题 6，2026-09-02 补充，结论完全准确）：`InsetComboBox` 模板选中框的 `ContentPresenter` 缺 `ContentTemplateSelector` / `ContentStringFormat`，闭合态绕过了项模板；补 `ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"` 与 `ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"` 两行。
- 【真机 09-04】[Android] **偏好页两条自动写入事实行不再互相顶替**：`InboxDelivery.lastApplyOutcomes` 原只保存最后一次写入结果（文本或图片），收到图片再收到文本时「图片自动写入」行消失、「自动写入剪贴板」行出现。改为 `InboxApplyOutcomes(text, image)` 按类别各留一槽，`recordApply` 只更新对应槽，`PreferencesRuntimeFacts.lastInboxApply` / `preferencesStatusLines` 两路读取。测试：`InboxDeliveryTest.textAndImageOutcomesKeepTheirOwnSlots`、`PreferencesStatusTest` 两行并存例。
- 【真机 09-04】[Android] **本机读取状态行不再外露原始错误码**（真机显示「首选 特权直读 自 08:45 起未就绪（PRIVILEGED_CHANNEL_OFFLINE）」，违反设置路线图 §4.3「UI 永不出现实现名词 / 错误码」）。新增纯函数 `ReadRouteReasons.phraseFor`：把各路线探测码与运行时健康码（`PRIVILEGED_* / PRIV_HOST_* / CLIPBOARD_BINDER_DEAD`、`READ_LOGS_NOT_GRANTED / ADB_LOG_*`、`OVERLAY_* / BATTERY_OPTIMIZATION_RESTRICTED`、三种「未实测」码）映射为短语（「特权通道未运行」「特权通道未授权」「与特权通道的连接已断开」「READ_LOGS 授权已失效」「悬浮窗权限未开启」「屏幕未亮起」「电池优化未放行」「尚未完成实测」…；READ_LOGS 作为权限名保留）；未知码不再加括号，改用 `read_live_degraded_fallback_plain` / `read_live_degraded_not_ready_plain`；「当前没有读取路线在运行（…）」同样走短语、未知则「原因未知」。14 短语 + 2 plain 键 × 19 语。测试：`ReadRouteReasonsTest`、`ReadRouteStatusTest` 回退例。
- 【真机 09-04】[Android] **特权宿主在应用附着后不再每秒重发 binder**（logcat `ShizukuProvider: sendBinder is called when already a living binder` 每秒一条持续 20 秒以上）。根因在我方 `PrivilegedHostService` 的重发循环：`attachApplication` 成功时把 `resendTicks` 归零，等于把节奏重置回 1 秒快档再跑 30 次。抽出 `BinderResendPolicy`：无客户端附着时 1 秒 × 30 次后退到 10 秒（宿主先于应用启动、或推送本身拉起应用），已附着后 30 秒保活；附着即刻改期（在 looper 线程上 remove + post，正在执行的 tick 不会留下第二条循环），客户端死亡则归零重回快档，替代进程一秒内拿到 binder。测试：`BinderResendPolicyTest`。
- 【真机 09-04】[Windows] **手机在批准前取消配对时诊断日志现有结束码**：此前日志止于 `peer_pairing_confirm_received`。`PairingService.ConfirmAsync` 对 `ApproveAsync` 抛出的非超时 `OperationCanceledException`（Kestrel `RequestAborted`）记 `PairingConfirmFailed(PAIRING_ABORTED)` 后继续抛——响应已无人接收，返回值与协议不变；`PairingErrorCodes.PeerAborted` 仅进 `All`（诊断允许列表），不在 `PairingJson` 的线格式校验与 `protocol/` 之内，诊断码 `peer_pairing_confirm_failed_pairing_aborted`。测试：`PairingServiceTests` 中止 / 超时各一例（互不误报）、`DiagnosticsLogCodesTests`。
- 【真机 09-04】[Windows] **通路页设备行「Last seen …」本地化**：`PairedDeviceViewModel` 两处英文字面量改为 `Device_LastSeenFormat`「最近在线 {0}」与 `Device_NeverConnected`「尚未连接过」，19 语。
- 【真机 09-04】[Windows] **一键放行对「安全警报点过取消」的机器不再无效：放行同时删除 Windows 为本程序生成的阻止规则**（ADR 0006 §2 末项）。真机复现：Windows 11 公用配置文件、监听 47654 时弹出的「Windows 安全中心警报」被取消后，系统为 `clipsync.app.exe` 生成两条入站 Block 规则（名 `ClipSync.App`，TCP/UDP 各一条，Public，LocalPort=Any）；应用内检测如实报出，但「放行」创建 `ClipSync TCP 47654` 后重新检测仍是 `firewall_inspect_no_rule`——防火墙里 Block 优先于 Allow，手机依旧连不上。修复：`FirewallVerdictEvaluator` 把「启用的入站 Block 规则且程序 = 本 exe」单列为 `FirewallReport.ProgramBlockRules`（名称 + 防火墙记录的原样路径，任何配置文件/协议，去重），`FirewallRuleCommand.Allow(profiles, blockRules)` 生成多步命令：每条去重后的规则一行 `netsh advfirewall firewall delete rule name="<名>" dir=in program="<记录的路径>"`（`program=` 用记录的路径而非 `Environment.ProcessPath`——安全警报把路径写成全小写；按 name + program 过滤保证只删 Windows 生成的那对，用户自建的同名/无程序端口 Block 规则照旧只列不删），再接原 add。`FirewallRuleElevator` 把多条命令写成临时脚本 `%TEMP%\clipsync-firewall-<guid>.txt`（每行一条无 `netsh` 前缀的命令，**UTF-8 无 BOM**——实测 netsh 脚本读取器只认这一种），以 `runas` 启动 `netsh.exe -f "<脚本>"`，**一次 UAC** 跑完，退出后删除脚本（无阻止规则时仍是单条 add 走 argv）；实测 `netsh -f` 某行失败后继续执行余下各行、任一行失败退出码 1，结果判定（退出码 → 三态 → 重新检测）不变；「如何启动」抽成可注入的 `IElevatedProcessLauncher`。诊断码新增 `firewall_rule_add_with_block_cleanup_applied / _cancelled / _failed_<code>`（与 `firewall_rule_add_*` 并列）、`firewall_rule_script_cleanup_failed_<type>`。主页面只读命令框、「复制命令」、PowerShell 等价行（追加 `Remove-NetFirewallRule -DisplayName "<名>"`，仅展示）与确认窗都列出全部命令（每行 `netsh ` 前缀），确认窗正文追加 `FirewallRule_Window_BlockNoteFormat`「同时会删除 Windows 安全警报被取消时为本程序生成的阻止规则：{0}。它们的存在会让任何放行规则失效。」；名称含 `"`/换行的规则无法写进 netsh 行，跳过并在结果行以 `FirewallRule_Result_BlockSkippedFormat` 说明请手动删除。2 新键 × 19 语。测试：`FirewallRuleCommandTests`（1 条 / 同名 TCP+UDP 对折成 1 行 / 多条不同名逐行精确断言、含空格路径加引号、含引号名称跳过、无阻止规则仍单条、显示串与 PowerShell 串）、新增 `FirewallRuleElevatorTests`（假启动器：System32 netsh 路径、`-f <脚本>`、脚本字节 = UTF-8 无 BOM 的 `ToScriptText()`、非 ASCII 名称、执行后/UAC 拒绝后/退出码非零后脚本均已删除、诊断码）、`FirewallVerdictEvaluatorTests`（安全警报的 TCP+UDP 对折成一条且保留记录路径、禁用/出站/他程序/无程序端口 Block 不入列）、`MainViewModelFirewallTests`（检测到阻止规则后预览与 PowerShell 串多出 delete 行并触发 PropertyChanged、确认窗请求携带规则、移除请求不携带、跳过名称写进结果行、取消时不提）。`docs/install.md` §3「之前点过取消」旁注明应用内放行会自动处理。
- 【真机 09-04】[Windows] **防火墙检测不再把 Store / 打包应用的规则当成全局放行**。真机复现：公用配置文件开启、`ClipSync TCP 47654` 只放行专用，检测却报 `firewall_inspect_allowed`，手机实测仍连接超时。原因是本机存在 Microsoft Store、ChatGPT、Xbox Game Bar 等打包应用的入站 Allow 规则——协议 Any、端口空、程序空、三配置文件全允许——它们只对各自应用包的流量生效，但 `INetFwRule` 基础成员读不出这层作用域。修复：`FirewallInspector` 经 IDispatch 追加读取 `INetFwRule3.LocalUserOwner` / `LocalAppPackageId`（读不到的旧规则对象按"无作用域"处理），`FirewallRuleInfo.IsScopedToUserOrPackage` 为真的规则在评估器里既不算放行也不算阻止。测试 `StorePackageRulesScopedToAUserOrPackageNeverCountAsAllowing`（用户 SID / 包 SID / 两者）与 `StorePackageBlockRulesDoNotCountAgainstTheListenerEither`。
- 【真机 09-04】[Windows] **规则已存在但被程序级阻止规则压制时，「放行」保持可用并先删旧规则再建**：`CanAllowPort` 改为 `!Busy && (!ManagedRuleExists || ProgramBlockRules.Count > 0)`，每次检测后重新通知；`FirewallRulePromptRequest.ReplaceExisting` 让脚本首行 `delete rule name="ClipSync TCP 47654"`，避免 netsh 在同名下建出第二条。真机：安全警报再次被取消后阻止规则重新出现，此时按钮可用，一次 UAC 后 `firewall_rule_add_with_block_cleanup_applied → firewall_inspect_allowed`，规则为 Private,Public 各一条。测试 `ReplaceExistingDeletesTheAppsOwnRuleFirstThenTheBlocksThenAdds`、`ReplaceExistingWithoutBlocksIsStillOneScriptedDeleteAndAdd`、`ManagedRuleShadowedByProgramBlocksKeepsAllowReachableAndReplacesItself`。
- 【真机 09-04】[Android] **Activity 重建后打开「蓝牙备援」崩溃**（`IllegalStateException: Attempting to launch an unregistered ActivityResultLauncher`，真机复现：切换输入法触发重建后在通路页打开开关；旋转、改系统语言同理）。`PreferencesViewModel` 的工厂 lambda 捕获了首个 `MainActivity` 实例的权限启动器，ViewModel 跨重建存活后仍调到已注销的启动器。修复：ViewModel 不再持有任何 Activity 回调——新增 `HostRequest`（蓝牙权限 / 通知权限 / 安装 APK / 未知来源设置）经 `Channel(BUFFERED)` 暴露为 `hostRequests`，当前 Activity 在 `onCreate` 里 `repeatOnLifecycle(STARTED)` 收集并用本实例的启动器执行；同路径下开机恢复 / 服务总开关请求通知权限、保留期清理（迁入 `viewModelScope`）、权限事实重采样（VM 自有 `refreshRuntimeFacts()`）一并脱离旧实例。行为差异：未配对时在偏好打开服务总开关也会询问通知权限；安装器请求在应用处于后台时缓冲到回前台再弹。测试 `PreferencesViewModelTest` 蓝牙有/无权限、通知权限三入口、Robolectric 真实 Room 保留清理。
- 【真机 09-04】[Windows] **图片同步事实行在对端已协商图片时不再说「手机端也开启时才互传图片」**：`SettingStatusMapper.ImageSync` 在 `ImageCapableDeviceCount ≥ 1` 时改用 `Status_ImageSync_OnImageCapableFormat`「已开 · {0} 台设备已连接，图片互传可用」；`0` 仍为 PeerTextOnly、未上报（null）仍为原句。19 语；`SettingStatusMapperTests` 随改。
- 【真机 09-08】[Windows] **开机自启事实行在开启后不再误报「未能登记启动项」**：`MainViewModel.OnLaunchAtStartupChanged` 在 App 尚未写注册表时就按旧的 `startupRegistry` 快照算状态，真机开启后行文案是失败句、注册表里却已写入。`App.OnViewModelPropertyChanged` 在 `ReconcileLaunchAtStartup` 之后调用 `RefreshStartupStatus()`，事实行改为陈述写入的实际结果；真机开 → 「已登记 · 下次登录随 Windows 启动」（HKCU Run 有 `"…\ClipSync.App.exe" --minimized`）、关 → 键删除、行文案回落。
- 【真机 09-08】[Windows] **托盘「退出」后进程可能永不结束（退出死锁）**。真机：一台跑了 15 小时、跨过 4 次 Modern Standby 的实例点退出后 UI 线程停在 `App.OnExit → syncHost.DisposeAsync().GetAwaiter().GetResult()`，其余线程没有任何 ClipSync 帧，进程一直活着（托管栈由 `dotnet-stack` 抓取）。两处修复：① `OnExit` 的三处异步释放改经 `DisposeOffDispatcher`——在线程池上运行、`Task.Wait` 上限 15 秒，超时记 `exit_dispose_timeout_<what>`、异常记 `exit_dispose_failed_<what>_<类型>` 后继续退出，UI 线程再也不会在退出路径上无界阻塞；② `SyncResilienceController` 的恢复通道加 `RecoveryTimeout`（默认 30 秒）：回调拿到的 token 在超时/释放时取消，`Task.WaitAsync` 兜底把不看 token 的回调**放弃**（门闸释放、计数 `TimedOutRecoveryCount`、钩子 `onRecoveryTimedOut` → 诊断码 `peer_recovery_timed_out`），下一次唤醒/网络信号照常恢复；`DisposeAsync` 等门闸同样有界。此前一次卡住的恢复会**静默关掉此后所有恢复**并让退出永远等待。配套：UDP 信标单次发送加 5 秒上限（`peer_beacon_send_timed_out`）。修复后真机复测：退出 → 进程 110 毫秒内退出码 0；退出期间连点托盘 30 次只留 `tray_open_refused`，无 `unhandled_*`、无崩溃转储（原报告的托盘崩溃在真机上确认已修）。测试 `AWedgedRecoveryIsAbandonedAfterTheTimeoutAndLaterSignalsStillRecover`、`DisposeDoesNotWaitForeverOnAWedgedRecovery`。
- 【真机 09-08】[Android] **悬浮窗轮询不再让前台应用的输入法每秒闪一次**（用户报告：QQ 转发、B 站分享等场景键盘持续闪烁）。根因：悬浮窗读取路线每 0.9 秒把 1×1 透明窗口切成可获焦以通过系统的剪贴板焦点检查，而一个可获焦窗口默认也是输入法客户端——每次获焦，系统就把它当作没有编辑框的新输入目标（`startInput reason=WINDOW_FOCUS_GAIN, inputType=0x0`），键盘随之收起；焦点弹回前台应用后再重新绑定编辑框，键盘再弹出。真机 `dumpsys window` 采样：修复前 80 次采样里 6 次输入法目标落在我方窗口上（与获焦次数一一对应）。修复：读取窗口加 `FLAG_ALT_FOCUSABLE_IM`（"取窗口焦点但从不与输入法交互"），并把窗口 `softInputMode` 设为 `SOFT_INPUT_STATE_UNCHANGED` 作第二道防线；空闲态（`FLAG_NOT_FOCUSABLE`）不加该标志——两者叠加会反转成"可用输入法"。修复后同样采样：获焦 6–7 次，输入法目标落在我方窗口 **0 次**，键盘弹出期间 `startInput` 事件 0 条；剪贴板读取照常（复制新文本后手机历史即刻出现）。测试 `the focused read window never presents itself as an input-method client`（读取窗口必带、空闲窗口必不带）。
- 【真机 09-08】[Windows] **睡眠/唤醒通知现在可观测**：同一实例 15 小时内内核电源日志记了 4 次 Modern Standby（Kernel-Power 506/507），应用诊断里却没有一条 `peer_suspend_sessions_gated` / `peer_resume_recovered_*`，而注册失败此前是静默的。`Win32SuspendResumeNotificationSource.RegistrationStatus` 暴露 `PowerRegisterSuspendResumeNotification` 的返回；`WindowsSystemStateEvents` 启动时记 `power_notify_registered` / `power_notify_register_failed_<code>`，每次去重后的信号记 `power_suspend_signal` / `power_resume_signal`。本机注册成功，投递与否留待下一次自然待机验证（见已知限制）。
- 【真机 09-11】[Windows] **超过 1 MiB 的 ASCII 剪贴板不再被记成「捕获故障」**：原生读取上限是 2 MiB UTF-16（含 NUL），策略上限是 1 MiB UTF-8。对 ASCII，「刚好超限」的正文无法物化进托管堆，适配器抛 `InvalidDataException`，诊断写成 `adapter_fault_Read_ClipboardAdapterException`，主窗走赭色「捕获故障」而不是章程要求的超限横幅 / 托盘气泡。修复：HGLOBAL 超过原生上限时不 lock、不拷正文，快照带 `ExceedsCaptureBudget`，策略按 `TooLarge` 拒绝；诊断故障码改记 `ErrorType`（不再永远是包一层的 `ClipboardAdapterException`）。单测：`ClipboardDataAccessorTests` / `Win32ClipboardAdapterTests` / `ClipboardCapturePolicyTests`。
- 【真机 09-11】[Android] **已配对时通路状态条不再说「尚未与电脑配对」**：任一段 `NEEDS_ACTION`（例如悬浮窗未授权）都复用 `conduit_band_blocked_sub`。网络段在已配对但不可达时是 `DEGRADED`，真正 beckon 的是本机读取，条上却写成没配对。新增 `conduit_band_blocked_paired_sub`「有环节需要你处理 · 轻触查看」，`ConduitBandCopy.of(needsAction, allReady, paired)` 按是否已配对选句。同轮：向导报告锚点在 `PrivHostErrorHints` 没有提示时回落 `ReadRouteReasons.phraseFor`，`OVERLAY_PERMISSION_MISSING` / `READ_LOGS_NOT_GRANTED` 不再只剩机器常量。1 新键 × 19 语。单测：`ConduitBandCopyTest` / `RouteErrorHintsTest`。
- 【第二批】[Windows] **PNG 编码器 `stackalloc` 栈溢出（真实崩溃路径）**：`ImageCodec.EncodePngBgra` 的 `WriteChunk` 原来 `stackalloc byte[type.Length + data.Length]` 为整段 IDAT 算 CRC；剪贴板监听线程默认 1 MiB 栈，复制一张压缩后 ≥ ~1 MiB 的截图（1024×512 噪声图即可复现）进程以 `0xC00000FD (Stack overflow)` 退出、栈顶正是 `WriteChunk`。修复为堆上按精确尺寸一次分配、查表 CRC-32 增量计算、逐行喂 `ZLibStream`；输出字节与改前逐字节一致（`ImageCodecEncodeTests` 4 组 SHA-256 golden 用旧编码器独立重算后相同，去重与像素摘要回声守卫依赖的 PNG 字节没有漂移）；1024² 39.5 ms / 7.76 MB → 32.7 ms / 1.04 MB，1600² 105.4 ms / 16.3 MB → 74.1 ms / 2.09 MB。回归测试 `EncodesMultiMegabyteIdatOnADefaultStackThread` 在 1 MiB 栈线程上覆盖。

### 文档

- 新增 `docs/field-test-report-v0.2.0.md`：2026-09-01 真机测试反馈原样归档（原在仓库根目录 `00-ClipSync-v0.2.0-真机测试总结.md`，移入 `docs/`），文末追加第六节「核实与处置」逐条给出成立 / 部分成立 / 属实的结论、处置与未采纳建议的理由。
- 新增 `docs/adr/0006-firewall-rule-management.md`（状态：已接受）：只读检测三态、先展示命令再 UAC、配套移除、按端口不按程序路径（便携包移动目录与应用内更新替换目录后按路径的规则会静默失效；代价是端口被占退到随机端口时规则不覆盖，检测把它列为「未发现规则」的原因并提示）、透明度折衷（UAC 显示的是微软签名的 `netsh.exe` 且不显示参数——以应用内先展示完整命令 + 复制自行运行缓解；自提权 + COM 写规则方案因发布包未做代码签名会出现「未知发布者」横幅而暂缓）、被否决的方案（安装器、启动时自动加规则、同时放行 UDP 47653、只写文档）。
- 新增 `docs/privacy-and-risks.md` / `.zh-CN.md` / `.ja.md`（见「新增」）。
- **`docs/install.md` §3 与 README 三语的防火墙说明修正**：去掉「放行 UDP 47653」的要求——复核 `UdpDiscoveryBroadcaster`，Windows 端 UDP 47653 只发不收、Android 端没有任何监听，入站放行只需 TCP 47654（多要一条规则既扩大暴露面又增加操作）；补「弹窗不出现 / 当前账户不是管理员 / 之前点过取消」时的手动 `New-NetFirewallRule` 命令与验证命令、网络类型检查（Windows 11 把新 Wi-Fi 默认归为公用，`Get-NetConnectionProfile`，两种改法及公用放行的代价）、点过「取消」留下的 Block 规则查删、`Test-NetConnection` 本机回环不能证明手机能连上、应用内放行 / 移除的位置；第 4 步卸载说明改为如实表述（开机自启项、防火墙规则两处按需清理）；排障表同步。README 三语 Windows 安装步骤同款改写。
- **`docs/install.md` §5 代理小节扩为「Clash / Surge / v2rayN 等代理时（TUN / 全局模式必读）」**（真机报告问题 5，属实）：系统代理已被两端显式绕过（Android `Proxy.NO_PROXY`、Windows `ClientWebSocket.Options.Proxy = null`），但 TUN / VPN / 增强 / 全局模式在 IP 层接管、应用绕不开，症状与防火墙拦截几乎一样，两端都要配；补 v2rayN / xray（`ip,<网段>/24,direct`、`tcp:47654 → direct`、绕过局域网）与 v2rayNG 的做法，排障表同步。
- `docs/threat-model.md`：主表补「接收端剪贴板暴露：同步到达后写入对端系统剪贴板」（对端任何前台应用 / 网页粘贴处理器 / 获系统授权的应用都可读到已同步正文直到被覆盖；缓解为私密模式、暂停、可关闭的自动写入与用户教育）与「防火墙按公用配置文件放行 TCP 47654」（公共 Wi-Fi 上同网段设备可探测到 `/v1/peer/health` 与 `/v1/pair/confirm`；应用内放行默认仅专用、health 只返回协议版本 / 端口 / 设备 ID / 写入状态词、进入会话需 pair secret 与挑战响应、confirm 按远端地址限速且需一次性令牌加本机批准）两项，剩余风险各补一句；「隐私默认值」第一条「只处理纯文本」自 2026-08-28 图片同步默认开（ADR 0004）起已过时，改为「只处理剪贴板里的文本与 PNG/JPEG 图片（原始字节含 EXIF 等元数据，不做剥离；其他图片格式与任意文件不收发）」；文末新增修订记录。
- `docs/design/DESIGN-CHARTER.md` §5.9 补「措辞规范」：§5.9 保护的是事实与篇幅不得删，不是某种句式；措辞以正向的行为描述为准（写应用做什么、何时做），避免自我辩解式否认句（「绝不静默」「不会假装」——否认句不给用户信息增量，读起来像此地无银）；实质承诺照旧保留；同一承诺在一条用户路径上出现一次即可；零散风险认知归入使用前必读文档。`docs/settings-roadmap.md` §4 文案原则同步引用。
- `docs/manual-qa-checklist.md`：第 0 节「防火墙提示已按预期出现」改为「已确认 TCP 47654 入站放行（弹窗允许或手动规则），并记录网络类型」；第 1 节 Windows 补应用内防火墙检测 / 放行 / 移除的真机验收项；第 5 节补退出不崩溃与配对请求到达时闪烁 + 气泡 + 诊断码两项。
- 【第二批】`docs/performance-audit.md` 新增「2026-09-03 第二轮：协议编解码、图片分片与落盘、PNG 编码器」：测量方法（`[PerfFact]` / `ProtocolAndMediaPerfTest`，`CLIPSYNC_PERF=1` 才运行，改前数字由同一组基准注入 `3c2e029` 源码同机独立复跑）、双端改前 / 改后表、逐项审查结论（含 `stackalloc` 崩溃复现记录）、存储 10k / 50k 基线与 `EXPLAIN QUERY PLAN`、看过但未做的候选（`ReplayWindow` 摘要存字节、`ORDER BY` 尾键临时 B 树、FTS5、双端 base64url 规范性差异）、UI 层建议（只建议不实现）。
- 【第二批】`docs/manual-qa-checklist.md`：第 1 节补配对信标（手机配对页出现「已在当前网络上收到…信号」；防火墙未放行时提示升级为「几乎可以确定」）与等待页「取消」的真机验收项；第 2 / 3 节补 Windows 与 Android 各状态事实行、读取路线降级后回升的真机验收项。
- 【第二批】`docs/privacy-and-risks.zh-CN.md` / `.md` / `.ja.md`：「局域网上其他设备能看到什么」小节补一句——配对页期间手机会监听电脑的局域网信标（只收不发、只含设备 ID / 端口 / 证书指纹、离开配对页即停止），需要 `CHANGE_WIFI_MULTICAST_STATE` 普通权限；三语同步。
- 【第二批】`docs/install.md` 排障表「扫码后连不上」补一行：手机配对页若显示已收到电脑信号但仍超时 → 电脑防火墙。`docs/threat-model.md` 修订记录加一行：UDP 信标接收为只读、不回包，不新增暴露面。
- 【真机 09-04】`docs/manual-qa-results.md` 新增「2026-09-04 真机验证（第二批）」：环境、逐项通过表（清单条目 ↔ 诊断码 / 界面文案）、未覆盖项、发现并已修复的 6 项、测试方法备注（手机 IME 会把 `adb shell input text` 转成中文；`ime set` 触发 Activity 重建清空 `remember` 状态——均非应用缺陷）。
- 【第二批】仓库清理（`2095421`）：删除已失效的 `AGENTS.md`（其指定的子代理环境与模型在当前工具链不存在）；删除未跟踪的过期本地资产（`releases/` 首个正式版前的本地打包产物 55 MB、`tmp-*` 截图与日志、`spike-logs/`、两个只含空子目录的误建目录）；`docs/bluetooth-phase0-report.md` 附录 A 注明原始 spike 日志未入库、正文摘录即全文。

### 已知限制（本节改动尚未真机验证）

- 本节全部改动只经单元测试与本机构建验证，**尚未在真机上复现原报告场景回归**：批准窗口 `FlashWindowEx` 闪烁、托盘气泡（请求到达 / 超时）、「放行 / 移除」的 UAC 全流程（确认窗 → UAC → netsh → 重新检测）、Android 不可达失败的 errno 分类与同网段判定，均未在真机上观察过一次。`docs/manual-qa-checklist.md` 已补对应验收项，待下次真机 QA 勾选。
- `FirewallInspector` 的 `dynamic` COM 路径只在开发机上（防火墙处于**关闭**状态、判定为「已放行」）实跑过一次；「未发现放行规则」「无法判断」「Block 规则」「端口不匹配」等分支只由 `FirewallVerdictEvaluator` 的纯函数测试覆盖，未在开着防火墙的真机上核对过 COM 返回值的形态。
- 本轮新增与改写的文案（Windows 40 新键 + 3 改写、Android 13 新键 + 2 改写，各 × 19 语）除简体中文源文外均为机器辅助翻译，待母语复核（`check-i18n-parity.py` 只保证逐键齐全与占位符一致，不保证译文质量）。`docs/privacy-and-risks.md`（en）与 `.ja.md` 同理。
- 检测视野的既有边界（ADR 0006 §1 已明示，非缺陷）：「规则存在 ≠ 一定可达」——第三方防火墙、路由器 AP 隔离、VPN/TUN 接管都在检测之外，可达性最终以手机能否连上为准。
- 【第二批】本批改动此前只经单元测试、合并后的双端全量构建与一次 Windows 应用 `--minimized` 实跑验证；**2026-09-04 已在真机上验证一轮**（Windows 11 + Redmi Note 11T Pro / Android 13，同一 /23 网段，Windows 防火墙三配置文件均关闭；逐项证据见 `docs/manual-qa-results.md`「2026-09-04 真机验证（第二批）」）。**已验证通过**：二维码地址梯队排序（WLAN 在前、WSL 虚拟网卡在后）、配对信标（手机「已在当前网络上收到「DENG」的信号」，`pairing_beacon_started / stopped`）、等待页分阶段与秒计、批准窗口弹出并批准（`peer_pairing_confirm_received → peer_pairing_confirmed`）、等待页「取消」（手机回未配对态、电脑批准窗口自动关闭）、双向文本同步、PC → 手机图片同步、回环抑制（`capture_rejected_SuppressedWrite`）、防火墙状态行（关闭态判「已放行」）、语言下拉框闭合态、偏好副标题、Windows 屏蔽进程 / 自动写入事实行、Android 路线降级 → 「重新探测首选路线」回升、设备行「最近同步」、偏好页服务 / 捕获 / 敏感事实行；全程无 `unhandled_*`。**同日下午临时开启公用配置文件防火墙续测，另验证通过**：防火墙拦截下电脑不弹批准窗、手机约 15 秒内给出「已收到信标但连接超时——几乎可以确定是电脑防火墙」的分层提示；检测识别安全警报生成的程序级 Block 规则；一键放行（含 Block 清理）一次 UAC 后 `firewall_inspect_allowed`；Store 应用规则不再误判为放行；Android 蓝牙权限被拒事实行；路线状态行人话。**09-08 续测另验证通过**：开机自启开 / 关（注册表 + 事实行）、退出期间托盘竞态（30 次连点只留 `tray_open_refused`，进程 110 毫秒内退出码 0）、蓝牙开关崩溃修复装机复验（Activity 重建后打开开关弹系统权限框、不崩溃）、电脑端 90 秒批准超时（`peer_pairing_confirm_failed_pairing_timeout` 精确于第 90 秒、手机「电脑未在限时内批准」、气泡经 Windows 推送通知平台日志确认投递）、Android「连接被拒绝」提示（停掉电脑监听后手机 6 秒内给出「电脑上没有程序在监听该端口」）。**仍未覆盖**：呼出快捷键状态行（未开启故无事实行）；**Android「无路由」提示分支疑似误分类，交接后续 QA**——手机断网时系统层为 `ENETUNREACH`，界面却显示「连接被拒绝」而非无路由文案，且与代码预期（`UNKNOWN` 应不出提示）不符，疑为 OkHttp 包装丢失 errno 后落入按消息猜测的兜底，需开分类日志（`log.tag.ClipSyncPairing`）断网复现确证；**Modern Standby 是否向应用投递睡眠/唤醒通知尚无定论**——注册已确认成功（`power_notify_registered`），但 09-07 的实例跨 4 次待机没有收到任何信号，诊断码已补齐，待下一次自然待机后看 `power_suspend_signal / power_resume_signal` 是否出现，若仍无则需改用窗口句柄注册（`RegisterSuspendResumeNotification` + `WM_POWERBROADCAST`）复核。真机发现的 13 个问题已修复，见「修复」小节【真机 09-04】【真机 09-08】条目。
- 【第二批】UDP 配对信标在访客网络 / AP 隔离 / 阻断组播与广播的 Wi-Fi 下收不到属预期——信标只是提示与地址提速，收不到时配对回到原来的「按二维码地址逐个试连」路径，不影响成功率；「几乎可以确定是防火墙」的提示只在**收到信标却连接超时**时出现。
- 【第二批】`MulticastLock` 仅在配对页可见期间持有（Review / Submitting / Failed），离开配对页、配对成功或 ViewModel 销毁即释放；不在配对页时手机不监听 UDP 47653。
- 【第二批】「已连接的手机未开启图片同步 / 电脑端未开启图片同步」两条事实行的判据是会话协商到的线版本（v1 = 文本；v2 = 双方图片同步都开），切换图片同步开关到会话重拨之间约 1 秒内可能短暂显示旧结论。Windows「最近同步」时间未做（Windows 侧已有「最近确认」`LastAckText` 行，未另加）。
- 【第二批】本批新增文案（Windows 3 键、Android 5 键，各 × 19 语）除简体中文源文外均为机器辅助翻译，待母语复核。

## [0.3.0] - 2026-09-02

### 新增

- [双端] **内置更新**：偏好新增「关于」组，显示本机版本号，并对照 GitHub 仓库 `fraternidaddeng/clipsync` 的 `/releases/latest`（正式发布，不含预发布）决定是否需要更新。检查不自动下载；发现新版本后由用户点「下载并安装」。下载后先核 SHA-256（优先用 Release 资产的 `digest`，否则拉 `.sha256` 侧车），对不上即丢弃、不安装。Android 把已校验 APK 交给系统安装器（需允许本应用安装未知来源）；Windows 便携目录在退出后由助手脚本 `robocopy` 覆盖再重启（用户数据在 LocalAppData，不动）。版本比较沿用发布脚本的 `versionCode` 方案（`0.1.0-rc.2` < `0.1.0` < `0.2.0`）。开发默认版本戳为 `0.3.0`（Android `versionName`/`versionCode` 30099，Windows `Directory.Build.props` 的 `Version`）；打 tag 时仍由 `release.yml` 注入。19 语文案逐键齐全。测试钉死版本比较、GitHub JSON/sidecar 解析、资产挑选（不把 unsigned APK 当安装包）、ZIP 解包与助手脚本、两端检查更新的 UI 状态。**`v0.2.0` 及更早的安装没有这套检查器，升到本版必须从 GitHub Release 手动下载一次**；此后的正式发布即可在应用内完成。

## [0.2.0] - 2026-08-31

### 变更

- [双端] **远端图片自动写入剪贴板默认开启**（2026-08-28 产品裁决「无所谓啊，本来截图就是我自己截的，默认开开」，与图片同步同日、同「产品完整体验」理由；ADR 0004 再增补修订记录）：Android `SyncSettingsStore.autoApplyImages`（`sync.auto_apply_images`）与 Windows 偏好页 `auto_apply_images` 设置的产品默认由关改开——新安装收到对端图片即自动写入本机剪贴板；缺失/不可解析的持久化值按「开」解析（与 `image_sync` / `auto_apply_remote` 同规则），用户显式关闭过的持久化「False」仍被尊重（升级不推翻已选的关）。**只改产品默认，不改任何拦截与安全语义**：暂停同步仍双杀文本与图片的自动写入（Android `InboxDelivery.autoApplyImagesAllowed`、Windows `RemoteApplyDecision`），Windows 私密模式照旧一并停下自动写入，图片写入门与文本自动应用继续互相独立（关文本不连坐图片、关图片不连坐文本），回声抑制与 MIME 魔数/尺寸/哈希校验原样保留。测试随裁决对齐：Android `ImageSyncDefaultAlignmentTest` 改钉「auto_apply_images 默认开、坏值回落为开、显式 False 仍关、与文本闸互不连坐」并新增坏值回落例，`InboxDeliveryTest` 闸例改钉「默认即放行、显式退出关闸、暂停仍双杀」；Windows `MainViewModelBasicSettingsTests` 新增「默认开（与 Android 对齐）+ 显式退出跨重启存活」两例。文案核实：Windows `Prefs_Sync_AutoApplyImages_Desc` 与 Android `prefs_auto_apply_images_desc` 19 语本就只描述行为（「关闭后只保留在历史中」）、未声称默认值，无需改动（`check-i18n-parity.py` 照绿）；ADR 0004、`manual-qa-results.md`、竞品对比与 `ui_preview.html`（开关预览态）同步修订。
- [双端] **图片同步默认开启**（2026-08-28 产品裁决「图片同步这种功能应该默认打开，这是产品的完整体验，而不是蓝牙那种备选方案」；ADR 0004 增补修订记录）：Android `SyncSettingsStore.imageSyncEnabled`（`sync.image_sync`）与 Windows 偏好页 `image_sync` 设置的产品默认由关改开——新安装或从未持久化该键的安装开箱即同步图片；缺失/不可解析的持久化值按「开」解析（与 `auto_apply_remote` 同规则），用户显式关闭过的持久化「False」仍被尊重（升级不推翻已选的关）。**只改产品默认的选择倾向，不改任何安全语义**：`auto_apply_images`（远端图片自动写入本机剪贴板）维持默认关——收到的图片默认只入历史，自动改写本机剪贴板仍是隐私敏感的显式选择；MIME 魔数/尺寸/哈希校验、v1 会话拒图、以及 2026-08-25 修复的未接线宿主库层 fail-closed 闸（`SyncSessionOptions.ImageSyncEnabled`、`PeerSyncHost` 兜底闸、Android `SyncSupervisor` 构造缺省）一律保留——忘记接线的宿主仍表现为纯 v1 文本对端。测试随裁决对齐：Android `ImageSyncDefaultAlignmentTest` 改钉「默认开、坏值回落为开、显式 False 仍关、`auto_apply_images` 独立默认关」，Windows `MainViewModelBasicSettingsTests` 新增「默认开 + 显式退出跨重启存活」两例、`ImageSyncGateTests` 闸测试改名明示「产品默认开、未接线闸仍 fail-closed」；文案对齐：Windows 偏好页 `Prefs_Sync_ImageSync_Desc` 19 语由「默认关闭」改「默认开启」（`check-i18n-parity.py` 照绿；Android 偏好文案本就未声称默认值，不动），设计宪章 §5.9 hint、`ui_preview.html`、README、竞品对比、`manual-qa-results.md`（限制 5 对齐方向反转注记）与 ADR 0004 同步修订。

### 安全

- 全库密钥泄露审计（2026-08-27，结论：**未发现任何真实密钥入库**）：全历史（375 提交、含全部远端分支，非浅克隆）逐 blob 扫描私钥块与 GitHub/AWS/Google/Slack/Stripe/OpenAI/npm/Azure 凭据形态、全历史文件名扫描（`.env`/`*.jks`/`*.keystore`/`*.pem`/`*.key`/`local.properties`/`google-services.json` 等）、工作树关键词与高熵串复核、workflows 全历史核查（secrets 一律经 `${{ secrets.* }}` 引用，keystore 只在 `RUNNER_TEMP` 解码）、脚本与文档核查（签名材料只走环境变量，文档均为占位符）——命中项全部为有意设计的合成测试向量（协议 fixtures 的 `pair_secret` 拒收样例、单测共享向量、蓝牙 spike 的公开固定值），无一真实。无需轮换任何凭据。
- 预防性收紧：`.gitignore` 新增 `*.b64`（`docs/install.md` §10 生成的 keystore base64 中间产物此前不在拦截范围——`*.jks` 被拦但它的 base64 副本不被拦）、`.env`/`*.env`、`keystore.properties`、`google-services.json`；新增 `scripts/check-secrets.sh` 轻量守门（只扫已追踪文件：禁入库文件名形态 + 10 条高信号凭据形态，合成向量零误报）并接入 CI 常驻作业 `secret-scan`。

### 修复

- [Android] **特权直读读不到剪贴板的真正病根:manifest 注册了官方 Provider 而非 ClipSync 自己的子类**(承接下一条无线超时修复后的深挖;此缺陷 USB 与无线同时命中,是此前多轮 `PRIV_HOST_USERSERVICE_DEAD` 只治标的根因)。症状:通路页特权直读卡「特权通道可用(宿主 ping 通)」+「已授权本应用」两项全绿,却始终「特权读取服务已断开(PRIV_HOST_USERSERVICE_DEAD)」,首选路线被迫降级到悬浮窗轮询——特权直读实际上从未真正读到过一次后台剪贴板。根因:特权宿主拉起的 UserService 子进程(`com.clipsync.android:clipsync-clipboard`)要把自己的剪贴板 binder 交回主应用,唯一通道是 `ContentProvider.call("sendUserService")`(`PrivilegedUserServiceStarter.sendBinder` → provider → `UserServiceAttachGate.attach` → `Shizuku.attachUserService` → 宿主 `attachUserService` → 主应用 `connected` 回调);而只有 `ClipSyncShizukuProvider` override 了 `call()` 处理这个 method,官方基类 `rikka.shizuku.ShizukuProvider` 根本不认识它、返回不含 host binder 的空应答。但 `AndroidManifest.xml` 为 authority `${applicationId}.shizuku` 注册的 provider 类名一直写的是官方 `rikka.shizuku.ShizukuProvider`(自 2026-08-24 `d8ce056` 引入起从未指向子类)——于是自定义 `ClipSyncShizukuProvider` 永不被实例化,`sendUserService` 永远落空:子进程 `sendBinder` 拿不到 host binder 便 `System.exit(1)`,宿主端 UserService 槽等不到 attach、约 30 秒后超时销毁,主应用 `connected` 回调永不触发,读取一律回 `PRIV_HOST_USERSERVICE_DEAD`。因宿主进程本身是活的(`Shizuku.pingBinder()` 为真、卡片显示「特权通道可用」),此前排查一直被引向「宿主被系统回收/无线会话失效」的自愈重试与 UI 诚实化(见下方多条 `USERSERVICE_DEAD` 记录),绕开了真正的病根。修复:manifest 的 shizuku provider 改注册 `com.clipsync.android.platform.clipboard.shizuku.ClipSyncShizukuProvider`(`enabled`/`exported`/`multiprocess`/`permission`/`authorities` 全部不变——子类继承 `ShizukuProvider`,`sendBinder` 与 authority 行为原样保留,仅补上 `sendUserService` 的处理)。真机(device `xaga` / MIUI / Android 13)复现并定位:宿主经无线启动后 `pgrep` 确在跑、通路页两项前提全绿,读取仍 `PRIV_HOST_USERSERVICE_DEAD`;设备 `dumpsys package`、源码与 git 历史三方确认 authority `com.clipsync.android.shizuku` 此前绑定官方类、`ClipSyncShizukuProvider` 从未在任何 manifest 注册。回归钉死:新增 Robolectric `PrivilegedHostProviderManifestTest`——断言 `PrivilegedHostConstants.PROVIDER_AUTHORITY` 解析到 `ClipSyncShizukuProvider`(误改回官方类即红)。Android 全套单测 + ktlint/detekt 照绿。**端到端真机确认(2026-08-31,同一台 device `xaga`)**:带修复的 APK 原地覆盖安装(与已装包同为 debug 签名、指纹一致,故配对与历史零丢失)后,设备 `dumpsys package` 显示 authority 已绑定 `ClipSyncShizukuProvider`;从电脑启动宿主后点「测试后台读取」——**UserService 子进程 `com.clipsync.android:clipsync-clipboard` 首次真正存活**(修复前它拿不到 host binder 即 `System.exit(1)`,绝不可能出现在进程表),应用回报「后台读取测试通过(测试文本已清除)」,通路页「本机读取」由「后台读取可用(悬浮窗轮询)」转为「**特权直读**」、路线卡错误码消失并成为当前首选。这是该通道自实现以来第一次端到端跑通。
- [双端] **特权直读一键启动不再在无线调试下必然超时失败**（用户报告「特权直读无法通过无线连接」；结论：无线链路与权限均正常，死因是启动脚本自身太慢 + 电脑端 30 秒每命令硬超时的合谋，无线的额外传输延迟恰好把它推过线）。根因：`start.sh` 清理旧宿主的那段逐 pid 遍历 `/proc/[0-9]*/cmdline`——每个 pid fork 两次 `tr`（读 cmdline 再读 comm），在 861 个进程的真机上仅此循环就要 26.7 秒，整个脚本 USB 下 24.2 秒、无线调试下 30.6 秒（TLS + Wi-Fi 每次往返更贵）；而 Windows 端 `ProcessAdbRunner` 对每条 adb 命令一律 30 秒超时、超时即杀进程树——无线启动恰好越线：adb 被杀在遍历中途，`app_process` 一行从未执行，宿主真的没启动，卡片报「adb 超时」；USB 以约 5 秒余量侥幸通过,造成「USB 行、无线不行」的假象。同款遍历还住在宿主拉起/销毁 UserService 的 `killByNiceNameCommand` 里，顶着自己 30 秒的 `USER_SERVICE_START_TIMEOUT_MS` 擦线。修复 [Android]：两处遍历改为一趟 `pgrep -f`（与 Windows 端宿主存活探测同一依赖，Android 10+ toybox 恒有），`clipsync_priv_server` 与 `$PACKAGE:clipsync-clipboard` 各查一遍——`-f` 匹配完整 cmdline，改名后的 argv0 与启动中的 `--nice-name=` 形态同旧遍历一样全覆盖；个别缺 pgrep 的 toybox 回退旧遍历（顺带删掉遍历里永不匹配的 comm glob 死分支：内核 comm 截断 15 字符，`clipsync_priv_se*` 这个 16 字符模式对 `clipsync_priv_s` 永假）。真机（861 进程）实测：USB 24.2s→0.26s，无线 30.6s→0.24s（mDNS 服务名与 `IP:端口` 两种 serial 形态都验证），杀旧宿主/拉新宿主/pgrep 复核存活全部照旧；脚本由 `ClipSyncApplication.onCreate` 每次进程启动重写，升级 APK 后首次打开即落地。修复 [Windows]：`IAdbRunner.RunAsync` 增加按命令超时；`StartAsync` 对启动脚本这一条给 120 秒专用预算（旧 APK 的慢脚本 + 慢无线仍能跑完，不必等用户升级 Android 端），探测/配对/连接等其余命令维持 30 秒快超时；超时被杀时不再丢弃已捕获的输出——脚本已打出「info: spawned」而传输随后挂死的场景，照旧进入 pgrep 复核而非直接宣判失败。测试钉死：Android `PrivilegedHostScriptTest` 新例（pgrep 是主路、遍历只许出现在 `command -v pgrep` 检查之后 + UserService 清理同款断言），Windows `PrivilegedHostStartVerificationTests` 新例（启动命令带扩展预算、pgrep 探测保持默认超时）；Android 全套单测 + Windows 全套 849 例照绿。装入带修复的 APK 后复测（2026-08-31，同一台 device `xaga`）：清空宿主与 UserService 后**纯走无线**（`192.168.2.250:33545`）启动 0.207 秒、宿主存活，随后的后台读取实测通过——原报障场景「特权直读无法通过无线连接」已闭环。
- [Android] 特权直读「测试后台读取」补齐「宿主活着但读取服务已死」的应用侧自愈——完成上一轮（下条）遗留的 UserService 生命周期复审。上一轮让验证读 `readTextForVerification()` 在**会话为空**（死亡已被 death 回调观测、`session` 已置空）时经一次新绑定让宿主重拉子进程、等待接住重生；但当应用仍**持有一具尚未回收的陈旧会话**（子进程已死而 `linkToDeath` 回调滞后——冻结/被缓存的应用最典型，或系统回收子进程时未即时投递死亡通知）时，`session?.let(::readFromSession)` 会**立即**读回 `PRIV_HOST_USERSERVICE_DEAD` 而短路掉那条自愈路径，把只需在手机上再点一次即可恢复的情形，误导用户回电脑重跑启动命令。现验证读在前提就绪（宿主 ping 得通且已授权）却从持有的会话读回死亡码（`PRIV_HOST_USERSERVICE_DEAD`/`CLIPBOARD_BINDER_DEAD`/`PRIV_HOST_BINDER_DEAD`）时，先释放陈旧 binder 并 `unbindUserService()`，使随后的绑定真正抵达宿主、由**宿主自己**重拉 UserService 子进程（复审确认宿主 `addUserService` 见旧 binder ping 不通即 `destroy` 重建、经 `startUserServiceProcess` 以 `sh -c app_process` 拉起——全程不需要新的 adb shell），再复用同一条有界等待预算（约 12 秒）接住重生会话读出，**与验证读的等待去重、不二次傻等**；带一行明确日志说明为何自愈。安全与诚实边界一并钉死：授权闸（`diagnoseUserActionOrMismatch`）在自愈之前先行——撤销同意后即便手里攥着死会话也**绝不静默重绑/重拉**（如实回 `PRIV_HOST_NOT_AUTHORIZED`）；宿主进程本身没了（`presence()` 非 RUNNING）则如实回 `PRIV_HOST_NOT_RUNNING`、不空拉一个不存在的宿主；宿主在线但重生始终不落地则等满预算后如实回 `PRIV_HOST_USERSERVICE_DEAD`——凡真需要电脑的情形一律照旧走「复制启动命令 / Windows 一键启动」，不谎报自愈。自愈只住在用户显式发起、跑在工作线程的验证读上（被动健康探测按既有设计不主动重拉子进程，避免每次刷新都拉起进程），事件与热读路径保持非阻塞。用户可感知的恢复动作因此不变、只是更稳：读取服务自己死掉（宿主还活着）时，回手机**直接再点一次「测试后台读取」**即可自愈、无需电脑；仅宿主进程本身没了（手机重启/系统回收宿主/无线会话失效）才需电脑重跑启动命令——恢复文案与动作标签未变，无新增用户可见字符串（`priv_hint_userservice_dead` 只在自愈失败、即真需电脑时才显示，措辞照旧准确），`check-i18n-parity.py` 照绿。测试钉死：`ShizukuClipboardBackendTest` 四新例（陈旧死会话经宿主重生自愈成功且日志带码、宿主重生始终不落地则等满预算才诚实报死且已尝试一次、宿主进程已死如实报 `NOT_RUNNING` 且零重绑零静默重拉、撤销授权即便手里有死会话也零静默重拉如实报 `NOT_AUTHORIZED`）、`PrivilegedReadWiringTest` 新例（扁平适配层的验证读经委托自愈陈旧死会话读出重生）；`FakeShizukuRuntime` 新增 `respawnAfterUnbind` 建模「释放陈旧绑定→宿主重拉→新绑定在途」。`docs/install.md` §9「测试后台读取报特权读取服务已断开」一行随之改写：先在手机上直接再点一次测试即可自愈（宿主还活着时），仅宿主真没了才回电脑。
- [双端] 特权直读「测试后台读取」不再抢跑冷启动的服务绑定——修复用户报告「无线调试下电脑已启动宿主、手机读取测试仍报 `PRIV_HOST_USERSERVICE_DEAD`」的死循环。根因其一 [Android]（主因）：特权直读的 UserService 绑定天生异步——宿主先拉起子进程、子进程再把 binder 附回应用，`bindUserService()` 期间如实返回「绑定中」；但读取测试走的普通 `readText()` 对「绑定中」**立即**回 `PRIV_HOST_USERSERVICE_DEAD`——电脑端刚启动宿主（或路线尚未常驻、读取测试自己触发的冷绑定）时，测试必然抢在绑定落地前失败；而路线又只有读实测通过才转 READY/常驻，「验证永远赢不了绑定」的死循环成型。无线调试冷生成最慢、且离用户的自然操作节奏（电脑点启动→立刻回手机点测试）最近，故最常在无线场景踩中。修复：`BackgroundClipboardBackend` 新增 `readTextForVerification()`（缺省实现即 `readText()`，其余后端行为不变），特权后端实现为**绑定感知**的验证读——前提缺失（未装/未跑/未授权）与硬绑定失败仍即时如实报错、活体会话直读不等待，唯「绑定中」以 48×250ms（约 12 秒，稳居宿主自身 35 秒绑定超时之内）的预算等绑定落地再读，预算打满才回 `USERSERVICE_DEAD`；等待只住在验证路径（读取测试本就跑在工作线程、用户明确在等结果），事件与热读路径保持非阻塞。这同时补上了「宿主活着但 UserService 已死」的应用侧自动恢复：验证读发起的新绑定会让宿主重新拉起 UserService 子进程，等待恰好接住重生——用户的恢复动作从此就是「电脑启动宿主 → 手机**直接再点一次**测试后台读取」，无需中间仪式。`ClipboardSelfTest.runReadTest()` 改走验证读；扁平适配层原样转发到活体委托。其二 [Windows]（辅因）：`PrivilegedHostAssistant.StartAsync` 此前只信 start.sh 打出的「spawned」就宣布成功——脚本以 setsid 拉起进程后即退出，进程若旋即死掉（无线 shell 会话闪断的典型症状），电脑端仍报「已发送启动命令」，用户按提示回手机测试自然失败且无从知道该怪哪端。现启动后以 6×750ms 轮询 pgrep 复核宿主进程确实存活：见到进程即报成功（首查即中不再多等）；探针本身跑不动（个别 ROM 无 pgrep）不诬赖脚本、维持原判；轮询打满仍不见进程则以新状态 `SpawnedButNotDetected` 如实报「启动脚本已执行，但随后没有看到宿主进程存活——可能刚拉起就退出了」并给出无线先断开重连再启动的恢复步骤（新键 `Conduit_Privileged_SpawnedNotDetected` × 19 语，`check-i18n-parity.py` 照绿）。测试钉死：Android `ShizukuClipboardBackendTest` 五新例（验证读等出重生会话、打满预算才报死、前提缺失不等待、硬失败不等待、活体会话直读）、`PrivilegedReadWiringTest` 新例（扁平适配层的验证读经委托等出冷绑定而普通读仍如实失败）、`ClipboardSelfTestTest` 新文件三例（读测试经绑定感知验证读通过、真死通道仍诚实带码失败、种子写失败即停绝不读用户剪贴板）；Windows `PrivilegedHostStartVerificationTests` 新文件五例（进程永不出现→`SpawnedButNotDetected`、重试后出现→成功、首查即中不多等、探针不可用维持原判、脚本失败跳过复核）。Android 全套 787 测试 + ktlintCheck/detekt 照绿（ktlint 基线净减 11 条：本轮新代码零新增条目，顺带清掉此前已修但未重生成的陈旧条目，其余为行号漂移）；Windows Core 619 测试照绿、App 在 Linux 交叉编译零警告。`docs/install.md` §9 排障改一行增一行（读取测试会等服务上线的正确恢复顺序、Windows 新提示的含义与下一步）。
- [Android] 通路 · 特权直读卡不再对「服务已断开」谎报「前提已就绪 · 待实测」（用户报告：通路页红字 `PRIV_HOST_USERSERVICE_DEAD`「特权读取服务已断开」，同时特权直读卡却挂着 `PRIVILEGED_READ_UNVERIFIED`，并指出「无线调试这里实际是不可用的状态」——UI 没有诚实反映）。根因：特权直读路线的能力探测只看两件事——路线前提（`AndroidRouteProbes`：`Shizuku.pingBinder()` 宿主在跑 + 已授权）与「是否已实测通过」；但 `pingBinder` 只证明**宿主进程**活着，真正读剪贴板走的是宿主再拉起的 **UserService 子进程**（无线调试/adb shell 会话断开后它可能单独死掉，而宿主仍应答 ping）。适配层 `ShizukuClipboardBackend.probe()` 的兜底分支在「已授权但未实测」时一律回 DEGRADED + `PRIVILEGED_READ_UNVERIFIED`（卡面「前提已就绪 · 待实测」），**完全忽略上一次读取测试的失败结果**——`recordReadTest` 明明把 `UNAVAILABLE` + `PRIV_HOST_USERSERVICE_DEAD` 写进了 `ClipboardCapabilityStore`，探测却从不回读：于是每次读取测试失败（弹条短暂显示真错）后，`refresh()` 又把卡面刷回乐观的「前提已就绪 · 待实测」，失败被洗掉，正是用户看到的自相矛盾。修复让探测诚实回读实测结果：`ClipboardCapabilityStore.lastReadFailureCode(mode)` 仅在某路线上次实测记为 `UNAVAILABLE` 时返回其稳定错误码，`ShizukuClipboardBackend` 新增 `lastReadFailureCode` 注入并在「已授权但未实测」分支据此裁决——上次实测证明通道已死则如实回 `UNAVAILABLE` + 该错误码（如 `PRIV_HOST_USERSERVICE_DEAD`），从未测过才回 DEGRADED「待实测」；一次通过的实测（READY）仍压过任何陈旧失败记录。向导映射与卡面随之诚实：`CapabilityRoutes` 对「前提齐备但通道已死」的特权路线把主操作改为 `COPY_PRIVILEGED_START_COMMAND`（用电脑重跑一次启动命令即可恢复——USB 或无线调试皆可、Windows 端可一键），并**保留**「测试后台读取」作次操作，让「电脑侧重启 → 回来复测」不成死胡同；`CapabilityWizard` 进度行对 `UNAVAILABLE` 路线显示新文案 `wizard_route_unavailable`「通道暂不可用，见下方说明」（不再落到「前提已就绪」）且不再以流蓝呈现。头部「本机读取」段亦随之从「已授权 · 待实测（DEGRADED）」转为「尚未打通（NEEDS_ACTION）」并召唤向导，与事实一致。针对无线调试路径专门加强错误提示：`priv_hint_userservice_dead`（zh-Hans/en）改为点名「无线调试断开后常见；用电脑重新执行一次启动命令即可恢复（USB 或无线调试均可，Windows 端可一键启动），再点『测试后台读取』验证」。新键 `wizard_route_unavailable` × 19 语逐键齐全（`check-i18n-parity.py` 照绿）。回归测试钉死：`CapabilityProbeBackendsTest` 三例（已授权+已死记录→`UNAVAILABLE`+码、无失败记录→DEGRADED/`READ_UNVERIFIED`、已实测 READY 压过陈旧死记录）、`CapabilityRoutesTest` 两例（前提齐备+`USERSERVICE_DEAD`→主操作复制启动命令且保留读取测试、已授权未实测仍只待测）、新增端到端 `PrivilegedChannelHonestyTest`（真实适配层 + 共享能力库 + 读取恒失败的委托：读取测试打到死 UserService 后卡面停在 `UNAVAILABLE`/`PRIV_HOST_USERSERVICE_DEAD`、给出复制启动命令 + 读取测试两操作，且此后一次纯「重新探测」不把失败洗回待测）。诚实边界：能力探测按既有设计不在被动探测里主动 bind（避免每次刷新都拉起子进程），故「UserService 是否已死」的权威信号仍来自一次真实读取（读取测试或后台服务已在运行时的健康检查）；宿主一旦被电脑重启，卡面在下一次读取测试通过前会持续如实显示上次的死因（此时读取测试始终可点，故非死胡同）。本轮亦排查宿主生命周期是否有「无线调试断开即误杀 UserService」的代码级缺陷：宿主与 UserService 均以 `setsid` 脱离 adb shell 会话组启动（`PrivilegedHostScript`），设计上应能扛过无线断开，未见可在 JVM 复现的确定性误杀点，故不臆改宿主代码，仅修 UI 诚实性与恢复引导（真机上宿主随会话被 OEM 回收属系统行为，恢复路径即上述电脑侧重启）。
- [双端] 特权直读无线调试深查（用户报告「无线调试几乎不可用」；结论：无线链路本身可用，但两端各有一处「陈旧状态谎报健在」让它**看起来**死了，另有一处撤销同意的守门缺口），钉死四处缺陷。其一 [Windows] **adb「already connected」的残留会话谎报成功**：无线调试的端口随息屏/切网/重启漂移后，adb 的会话表仍对旧 `IP:端口` 答「already connected」，而底层传输早已死透——卡片据此宣布「已连接」，随后「检测手机」却报离线/无设备，用户被两句互相矛盾的话卡死。现 `ConnectWirelessVerifiedAsync` 对每个「already connected」用 `adb devices` 交叉核验，对不上就 `adb disconnect` 掉残留条目重拨一次真的，恢复动作明说（「检测到 adb 里残留的旧无线会话已失效，已自动断开并重新连接」）绝不静默。其二 [Windows] **无线会话掉线后卡片停留在陈旧「已连接」**：连接成功的端点现被记住，之后每次探测把设备列表和它对账——掉线（offline 或消失）即把状态换成「无线连接已断开」加恢复步骤（回手机无线调试页核对当前 IP:端口，改填重连即可，配对不用重来）；卡片主状态行对 `IP:端口` 形态的 offline 条目也改说「无线调试会话已失效」而非误导性的「重新插拔」（USB 场景的插拔建议原样保留）。其三 [Windows] **撤销 adb 同意/停止出示的蜜月缺口**：二维码等待→配对→连接的异步链此前只在少数步骤检查会话号，撤销同意后在途的链仍可能继续发 adb 命令、把状态行写回来。现每个 await 之后重查「会话号 + 同意闸」双条件（`WirelessChainAlive`），撤销即断——之后零 adb 调用、零状态复活，撤销同时忘掉已记住的无线端点。其四 [Android] **持久化的「已实测验证」盖过活体死亡**：路线卡的 READY 来自一次性读实测的持久化标记，而宿主 shell binder 健在（前置全过）但 UserService/剪贴板 binder 已死（手机重启、系统回收、无线会话失效的典型后果）时，探测仍宣称 READY——用户对着「就绪」的路线收不到任何事件，这正是「电脑连好了、手机还说宿主死了」互相指责的根源。现探测在 READY 前问一句活体后端的健康：正在运行的委托报出当前死亡码（`PRIV_HOST_USERSERVICE_DEAD`/`PRIV_HOST_BINDER_DEAD`/`CLIPBOARD_BINDER_DEAD`）即以该码降级（路线卡随即给出对应恢复提示），未启动（STOPPED）或健康的委托不推翻已验证状态，重连退避期的无码 DEGRADED 也不让卡片每秒翻脸。文案随修：`priv_hint_userservice_dead`/`priv_hint_binder_dead` 19 语从语焉不详的「重启通道」改为具体动作——先「重新探测」等自动重连，仍无效在电脑端重跑启动命令（Windows 一键「启动特权直读」），并点明通道不会在手机重启/系统回收后自愈；Windows 侧新增 5 键 × 19 语（会话已断开、残留重拨说明、离线无线诊断、pgrep 探针失败时如实说「暂时无法确认」而非谎报「未运行」等）。测试钉死：Windows Core 侧 verified-connect 四例（新鲜连接不多探测、列表背书的 already connected 放行、残留重拨、消失后重拨失败以重拨裁决为准）+ `WirelessSessionDiagnosis` 纯分类器五例（`IP:端口` 即无线序列号、Ready/StaleOffline/Vanished），App 侧 `MainViewModelWirelessTests` 七例（连接记忆→掉线陈述、残留重拨提示、失败指路、无线离线 vs USB 离线文案分流、pgrep 未知态不禁用启动按钮、撤销中断在途链且不复活会话记忆）；Android `CapabilityProbeBackendsTest` 四新例（健康/停止/无委托不降级、UserService 死亡降级带码、binder/剪贴板死亡同理、无码 DEGRADED 不翻脸）。`docs/install.md` §8.1.1 注意事项扩两条（端口漂移的两道防线、手机端状态要靠重新探测）、§9 排障改一行增两行（无线 offline 与 USB offline 分流、掉线恢复步骤、「电脑说启动了手机说没有」的对账顺序）。
- [双端] 剪贴板同步深查第三轮（图片链路第二轮修复后继续搜边角），钉死四处缺陷：私密模式的自动写入缺口、删除传播的双向互操作断裂、墓碑 outbox 的确认竞态、图片去重的元数据信任漏洞。其一 [Windows] **私密模式不再自动写入远端剪贴**：托盘「私密模式」承诺「捕获与同步全部停止」（语义强于 Android 的仅出站「本机复制的内容不离开这台设备」），但 `RemoteApplyDecision` 此前只看暂停不看私密——私密期间对端一复制，远端内容照样覆盖本机剪贴板，用户正要粘贴的东西被顶掉。现私密与暂停同权：停自动写入（文本与图片一并），接收入历史照旧；两端语义分歧作为有意设计写进策略注释。其二 [Android] **入站 `deleted` 墓碑此前对已收到的活体永不生效**（Windows→Android 删除传播互操作断裂）：Windows 删除已同步的剪贴后按既有设计重新公告该序列为 `unavailable/deleted`，但 Android 引擎对「已覆盖序列」一律只回 ack 不入库，且仓库层遇到同身份活体行也只答「已持有」——删除公告被确认掉却从未应用，重试也救不回（幂等短路）。现镜像 Windows store 语义：`unavailable` 头即使序列已覆盖也送进仓库；仓库遇同身份活体行且理由为 `deleted`（唯一来源权威的删除理由）时把活体软删为墓碑（正文/哈希抹除、向多对端场景 fan-out、重试幂等），`expired`/`local_only` 等其余理由一律保留本机已有内容。Android 自身的单条删除按产品文案维持仅本机（「不影响其他设备」），本轮只修入站方向。其三 [Android] **墓碑 outbox 确认竞态**（Windows 同款竞态 `LateLiveAckForContent…` 修复的 Android 半边此前缺失）：`applyPeerAckRanges` 无条件删除已确认范围内的 outbox 行——对端对旧内容公告的 ack 恰与本机删除赛跑时，会把尚未公告的墓碑 outbox 行悄悄清掉，删除从此无人再投递（对端向量已覆盖该序列，gap 补拉也不会再来）。现镜像 Windows 双 SQL：活体行任意状态皆可清；墓碑行只在自身公告已发出且来的是活跃 ack（`dropTerminalOutbox = true`）时才清，`known_vector` 覆盖证据（`= false`，会话恢复路径）永不清墓碑。其四 [Android] **图片哈希去重曾信任公告头的元数据**：对已持有字节的哈希重放公告，引擎此前把头里自称的 MIME/尺寸写进事件行与回调——恶意或错误的对端可为本机已验证的字节记下伪造元数据（如把 PNG 说成 JPEG），下游自动写入按 MIME 分派即错。现 `findLiveImageByHash` 返回字节自身验证提交时的元数据（本尊来自魔数嗅探与真实解码），去重提交与回调一律用它；仓库层再加「首写为准」守卫——`media_blobs` 元数据行一旦写入绝不被后续同哈希事件改写；墓碑化的图片行不再参与去重（防止已删除内容借哈希还魂，需真实重传）。回归测试钉死：Windows 私密模式策略例、Android 仓库层墓碑升级/理由白名单/ack 竞态三段舞/首写为准四例、内存仓库镜像三例、引擎层「已覆盖序列的 deleted 重公告仍墓碑化」例、真实 Room+blob 链路两例（deleted 重公告墓碑化后同哈希需真实重传不得去重还魂；伪造 MIME/尺寸的去重公告提交的是已验证元数据）；顺带排查未见缺陷即未动代码的项：传输中途关图片开关（会话反弹 + 传输态随会话丢弃 + ack 前原子提交，无半写）、缩略图失败（仅渲染期惰性生成，失败灰面占位，不碰传输）、bt1 蓝牙通路图片（恒 v1 拨号 + 出站降级 `local_only` + v1 校验器拒图片帧，无崩溃路径）、Android 图片自动写入默认关的诚实性（ADR 0004 fail-closed 属实）。Android 全套单测 + ktlint/detekt 照绿；Windows 侧 Linux 交叉编译零警告，WPF 策略测试随 CI 在 windows runner 复核。
- [Android] 偏好与通路两面「镜子」不再冻结在构建时刻——设置库的外部写入现在会推送到 UI（UI/服务/偏好边界排查轮）。其一，偏好页开关面陈旧：`PreferencesViewModel` 只在构建时读一次 `SyncSettingsStore`，而常驻通知的「暂停同步/暂停捕获/恢复」动作直接写库——应用开着时从通知栏拍下暂停，回到偏好页开关仍显示未暂停，轻点开关看似「从关拨开」实则写回同值，两个入口自此互相矛盾（同理适用于任何绕过该 ViewModel 的写入）。现补 `SyncSettingsChanges.changes()`（设置文件级变更 tick：SharedPreferences 监听器随收集注册/注销、conflated——监听器只在值真正变化时触发），ViewModel 逐 tick 走新的 `refreshFromStore()` 整面重读（导出/导入结果行为 ViewModel 自有态，重读不触碰）。其二，通路 · 本机服务段等不来「已停用」：`SyncHealth` 组合流此前只在四条服务流发射时顺手重读总开关——服务已停时再拨总开关（典型：FGS 提升被拒、服务已自停，用户于是想彻底关掉），`stop()` 是 no-op、四条流一条不动，重读永不再跑，段面卡死在陈旧「启动失败」上，手动「重新探测」也救不回（探测不重采样同步流）；段映射早已写好「已停用压过陈旧失败码」的分支，缺的正是这次发射。总开关升格为组合流的第五路输入（新 `syncHealthFlow` 工厂 + `MainActivity.serviceEnabledStates()`：当前值先发、每个设置 tick 重读、去重后入组合）。回归钉死：`SyncHealthFlowTest` 三例（服务流全静默时拨开关必产生新快照且陈旧失败码随行、蓝牙/IP 连接映射随工厂迁移一并钉住）、`SyncSettingsChangesTest` Robolectric 一例（写即 tick、取消收集即停）、`PreferencesViewModelTest` 两新例（通知动作外部写库后 `refreshFromStore` 对齐、settings tick 免 UI 动作自动对齐）；Android 全套单测（testDebugUnitTest）752 例照绿，ktlint/detekt 零基线新增。本轮同时排查未见问题、如实记录：快速连拨开关无竞态（`commit()` 同步落盘、门控每事件重查）、通知状态行诚实（暂停事实压过轮询事实、图片开关翻动即刷新动作标签）、服务停止后滞留通知动作已有守门（`refuseDisabledStart` + 既有 Robolectric 用例）、历史图片行/仅本机保留徽记/搜索与 Windows 详情窗、托盘暂停快捷键即时持久化、首跑引导与 `service_enabled` 缺省的关系均核查无恙。
- [双端] 例行 debug 第三轮（全套门禁先行照绿，随后按运行时缺陷主动排查图片链路与剪贴板写入路径），修四处资源与并发缺陷。其一 [Android] 回声抑制表的跨线程数据竞争：`ClipboardWriteCoordinator` 的抑制表（写入布防 → 捕获侧消费的自写标记）此前是无同步的普通 `mutableMapOf`，而生产拓扑恰好跨线程——主线程布防（自动写入、历史复制、通知「复制」动作），悬浮窗轮询后端却在自己的 `HandlerThread` 上消费（`shouldSuppress`/`shouldSuppressContent`，内含遍历整表的过期清扫与哈希查找），特权直读的 binder 回调线程同理；竞态窗口内轻则标记丢失（回声逃逸成重复捕获）重则遍历中途结构变更直接崩溃。现以专用锁串行化全部表操作（哈希计算与阻塞的 OS 写入仍在锁外），双线程 4000 轮写入×消费风暴测试钉死。其二 [Android] `clipboard-share` 目录无限增长：图片自动写入把每个事件的整幅编码字节落成 `filesDir/clipboard-share/<eventId>.png|jpg` 供 FileProvider 端出，但从无任何清理——每张应用过的图片永久占据内部存储，还跳过既有保留期清理（同一像素在历史与媒体库里到期会删，这个分身不删）。现在每次成功写入后修剪：剪贴板已指向新文件，被顶替的旧文件除 5 分钟宽限期（掩护正在进行的粘贴读取）外一律删除；纯 JVM 两例钉死（过期删/宽限留/当前目标凭身份永不删）。其三 [双端] 重复 `clip_payload_begin` 静默顶替在途传输：同一事件的第二个 begin 此前直接覆盖在途表项，原传输的半写临时文件连同打开的文件流被孤儿化（Android 泄流句柄；Windows 额外泄漏一个并发下载槽——两槽耗尽后该会话永不能再收图）。双端对称改为按协议失败（`MEDIA_OUT_OF_ORDER`/`begin_duplicate`，会话终止后既有清扫路径接管在途传输）；Android 会话级脚本传输测试 + Windows 真 Kestrel 脚本拨号端测试各一例钉死（断言错误帧、临时文件清零、事件未入库）。其四 [Windows] 会话结束不清扫半收图片临时文件：Android 的 `run()` finally 一直会 abort 在途传输并删临时文件，Windows 对应路径却只关流不删文件（`MediaBlobStore` 根本没有 abort API，且监听会话引擎从不被 Dispose）——任何中途断线（真机 Wi-Fi 抖动的常态）都在 `media\tmp` 留下 `.part` 尸体，要等下次应用重启且文件已满 24 小时才被 `RecoverTemps` 收走。补 `MediaBlobStore.Abort`（关流 + 删临时文件）并接入 `RunAsync` finally 与 `Dispose`；顺带双端补上 `clip_payload_end` 校验失败分支的同型孤儿（表项先移除再校验，校验不过时既有清扫已看不见它，现就地 abort）。双端全套（Android 750、Windows 597）+ ktlint/detekt（基线仅收行号漂移，零新增）+ i18n/secrets 门禁 + E2E stage4（含 ~2 MB 多块双向）照绿。
- [双端] 图片传输深查第二轮（会话粘滞与拨号竞态修复后用户仍可能有障碍），钉死两处只有真机才踩得中的链路缺陷。其一 [Windows] 应用 JPEG 的回环回声：远端图片自动写入与历史「复制」把存储的 JPEG 写进系统剪贴板时，回声抑制只按内容哈希布防——但 JPEG 写入只带 CF_DIB/CF_JFIF 不带 PNG 剪贴板格式，本端监听器读回的是 DIB→PNG 重编码（内容哈希必然改变），抑制落空，回声被当作全新本地捕获：历史多出一条重复 PNG，还会作为「新图」同步回手机。两处写入点（`App.OnRemoteClipsCommitted`、`MainViewModel.CopyImage`）改为同时携带像素摘要（`TryPixelDigest`——解码后逐像素哈希，与编码格式无关），`ClipboardCapturePolicy.EvaluateImage` 既有的摘要匹配路径由此真正生效。其二 [Android] 真实应用的剪贴板形状读不进来：`ClipboardMediaReader` 以 item 序号索引 `ClipDescription` 的 mime 列表——两者并无对应关系（描述 mime 是剪贴级列表），真实剪贴板上条目数多于 mime 数即 `IndexOutOfBoundsException`；且描述 mime 只认精确 `image/png`/`image/jpeg`——Chrome/相册常写 `image/` 通配或非标 `image/jpg`、文件管理器只写 `text/uri-list`，全被静默丢弃（图片开关开着却「复制了没反应」）。改为：描述提示放宽到任意 `image/` 前缀且整剪贴只判一次，URI 条目逐个问 resolver `getType`，最终裁决交给 `ImageCodec` 魔数嗅探（fail-closed：webp/gif 依旧拒收）；外层门 `ForegroundClipboardBackend` 换用新的 `clipLooksLikeImage`（描述像图或含任意 URI 即进读取通道）。回归测试钉死：策略层像素摘要抑制两例（含「只按哈希布防必漏回声」的反例）、App 层 JPEG 写入→DIB 读回像素摘要一致往返、Android 阅读器 Robolectric 8 例（`image/` 通配、`image/jpg`、uri-list 走 resolver、条目多于 mime 不崩溃、非 PNG/JPEG 字节 fail-closed 等）；双端全套测试 + 大图多块 E2E（`run-e2e-stage4.ps1`）照绿。诚实边界（CI 覆盖不到、真机才可复核）：Android 图片捕获只走前台通道——三条后台读取路线（特权直读/日志感知/悬浮窗轮询）与分享面板均为文本，复制图片时需 ClipSync 处于前台（既有设计边界，非本轮回归）；Android「远端图片自动写入」默认关（ADR 0004）——收到的图片一定入历史，但不开此开关不会自动进系统剪贴板；来源应用未授予 URI 读权限时按 fail-closed 静默跳过；Windows 侧超大截图（约 800 万像素以上的 32bpp DIB，如 5K/6K 整屏 PrtScn）受 `DibCodec` 32 MiB 分配守卫所限会静默不捕获，低于名义上的 3200 万像素预算——已知未修的权衡，真机独有。
- [Android] 图片同步开关的会话反弹补上「拨号窗口」竞态——上一条修复让开关翻动立即反弹活动会话，但若翻动恰落在一次拨号进行中（重连循环正在 `connect`，`activeEngine` 尚为空），`restartSession()` 无会话可关、其重连提示又会在下一次退避等待开始时被当作陈旧信号排掉：拨号若成功，会话仍以拨号前锁定的旧协议版本存续，开关再次「看起来没有用」。修复双层兜底：`SyncEngine.requestClose()` 改为粘性（先于 `run()` 到达的关闭请求在会话启动当刻兑现，不再依赖 `sessionJob` 已赋值），`SyncSupervisor` 新增 `restartRequested` 标记——请求落在拨号中则新会话建立后立即自弹重拨，标记在每次拨号读取连接期输入（含 `sync.image_sync`）的同一处清除，故被本次拨号满足的请求不会引发多余反弹。`SyncSupervisorTest` 新例钉死：门控拨号挂起期间翻开关 + `restartSession()`，拨号放行后旧版本会话立即反弹、一步退避后按 v2 重拨成功。
- [Android] 「后台同步服务」总开关补上服务自身的最后一道守门——静态入口 `ClipboardSyncService.start()` 已拒绝所有代码路径，但直达服务的 start intent 仍可绕过它：常驻通知动作按钮的 PendingIntent 与「关闭」竞速（关停瞬间点到即复活整个同步栈），或个别 OEM 重投递 start intent。现在 `onStartCommand` 自查 `sync.service_enabled`：关闭时按 `startForegroundService` 契约先短促提升再立即撤销通知并 `stopSelf`，返回 `START_NOT_STICKY`——关即真关，任何路径都不复活。Robolectric 两例钉死（直达 intent 在开关关闭时服务自停且不粘性；滞留的「暂停捕获」通知动作既不拉起同步栈、也不套用那次陈旧动作）。
- [Android] 「后台同步服务」总开关再堵一条复活路径（`sync.service_enabled` 排查轮）：配对完成「拨回开」的副作用此前按组合重放——配对成功页未点「完成」时 ViewModel 停留在 Paired 态，任何 Activity 重建（旋转、改语言、改外观）都会重放 `LaunchedEffect` 再次强制拨开总开关：用户配对后关掉服务，一次旋转就被拨回。现由 `PairingServiceStartPolicy` 钉死「每次配对仪式恰好拨一次」——已处理标记存进保存态跨重建，回到 Idle 重新武装、下次配对照常拨开；忘记配对仍只在对端真正消失时停服务。纯 JVM 策略五例钉死（首次拨开、重建重放不再拨、回 Idle 重新武装、只有真忘记才停、in-flight 步骤不动任何东西）。
- [双端] 图片同步开关翻动现在立即对活动会话生效——修复用户报告「图片功能的传输不知道在哪次更新中死掉了」。调查结论：图片传输管道本身完好（编解码、分块、v2 帧、双端小图 E2E 在 main 上全部通过；本次另补 ~2 MB 多块传输测试佐证，同样通过），「死掉」是两个事实叠加：其一，`v0.1.0-rc.1` 的 APK 未用发布签名、`rc.2` 起换为正式签名，升级被迫卸载重装、应用数据清空，`sync.image_sync` 回到默认**关闭**（ADR 0004 fail-closed，本身正确）；其二，**会话粘滞**——协议版本（v2 带图 / v1 纯文本）在拨号时一次锁定，用户重新打开图片同步后，已建立的 v1 会话不会自行断开重协商，稳定网络下可无限期粘着，开关看起来「没有用」，图片被降级为「仅本机保留」标记且永不补传。修复为双端对称的最小改动：Android 端 `ClipboardSyncService` 监听 `sync.image_sync` 键变化并调用新的 `SyncSupervisor.restartSession()`（关闭活动引擎；认证会话的关闭会重置退避，约 1 秒内按新偏好重拨——开则 v2 优先，关则回 v1；若正处退避等待则如网络恢复般立即缩短等待）；Windows 端 `MainViewModel` 新增 `ImageSyncEnabledChanged` 事件（值真正变化才触发），App 层借此调用新的 `PeerSyncHost.DisconnectAllSessions()`，手机的重连循环随即重拨并在 `/v2` 上握手成功。回归测试钉死：`SyncSupervisorTest` 两例（活动会话反弹后按新偏好重拨 v2；退避等待中重启立即生效）、`ImageSyncGateTests` 新例（v1 会话存续中翻开开关 + 反弹 → 重拨升级 v2、图片双向可达）、`MainViewModel` 事件单测（同值重赋不触发）；补齐当初缺席的真实尺寸覆盖——`LargeImageReproTest`（C# 双向多块）与 `LargeImageCrossClientE2eTest`（Kotlin ↔ C# 双向 ~2 MB PNG 真分块流式传输，已接入 `run-e2e-stage4.ps1`，E2eHost 为此新增第四个配对身份与确定性大图生成命令 `gen-large-png`）。
- [Android] 悬浮窗轮询不再让第三方全屏视频的虚拟导航键反复闪现（用户报告：哔哩哔哩横屏全屏播放时导航键每隔约一秒闪一次）。根因：轮询读剪贴板需要 1×1 悬浮窗短暂变为可聚焦（Android 10+ 剪贴板只对焦点窗口开放），而 Android 11+ 把系统栏显隐的控制权交给**焦点窗口**——该窗口此前从未申明任何系统栏偏好，缺省即「请求可见」，于是每次抢焦点都把沉浸式应用藏起的导航栏（和状态栏）拉出来一瞬，焦点归还后又缩回去，正好卡在 900ms 轮询节拍上。修复采用**逐栏镜像**而非一律隐藏（一律隐藏会把毛病反过来：普通应用里每个轮询周期系统栏会闪没一下）：读取窗口在变为可聚焦之前，按自身收到的 WindowInsets 采样当下真实系统栏状态，状态栏/导航栏各自独立镜像申明（前台藏则申明藏、显则申明显）——沉浸式视频里导航栏保持隐藏，普通应用里保持可见，两个方向都不被搅动；恢复不可聚焦时刻意不改申明（那一帧本窗口可能仍是控制目标，此刻改回「显示」会自己制造闪烁）。诚实边界：`WindowInsets.isVisible` 仅 Android 11+ 可用，Android 10（API 29）采样恒为未知、不触碰任何申明，行为与修复前逐位一致——若该版本亦有此症状，本修复不覆盖。镜像决策落在纯数据 `OverlayWindowSpec.hiddenBars` 上，新增 `OverlaySystemBarMirrorTest` 七例钉死策略（沉浸双藏、普通双显、仅导航栏藏只镜像导航栏、未知不触碰、空闲窗格永不带申明、采样先于抢焦点、读取抛异常仍安全恢复）；悬浮窗既有不变量（永不可触摸、1×1 全透明、停止即撤窗）原样保留。

### 新增

- [Android] 偏好 · 同步新增「后台同步服务」总开关——在应用内彻底关闭后台服务的入口（用户澄清「我指的关闭是直接在应用内部彻底关掉它」：「暂停自动捕获」只暂停捕获、服务仍常驻，通路页「停止服务」又非持久——重开应用即被 `MainActivity.onCreate` 的自动恢复重启，等于关不掉）。新键 `sync.service_enabled`（默认开，存量安装行为不变）由 `ClipboardSyncService.start()` 自身把守，所有启动路径（打开应用自动恢复、配对完成、通路「启动服务」、开机恢复及其健康检查）一律先过此键：关即真关——前台服务停止、后台读取后端随服务释放停转、常驻通知消失，重开应用与重启手机都不会自行恢复；应用打开期间的前台复制仍记入本机历史，重新开启后才继续同步；「暂停同步/暂停自动捕获」键位与语义原样不动（关闭与暂停分立，描述文案互相指认）。通路 · 本机服务段的「停止服务/启动服务」按钮改为与该开关同键互通（停止从此持久），段面新增灰面「已关闭」事实态（UNAVAILABLE——用户的选择不装作故障，并压过陈旧的启动失败码）；配对完成视作显式「要同步」时刻，会把总开关拨回开；开机恢复在开关关闭时不尝试启动、也不发「需要恢复」通知。4 新键 × 19 语逐键齐全（`check-i18n-parity.py` 照绿）；默认值与往返、ViewModel 先持久化再通知宿主、stop 不碰暂停键、开机恢复四条件、`start()` 守门（Robolectric 两例）、通路「已关闭」事实态与其对失败码的优先级由 8 条新单测钉死；`settings-roadmap` §2.1/§4.1 同步注记。
- [Android] 偏好 · 捕获新增「暂停自动捕获」开关（用户反馈「本身是强制监听后台，但是手机端却没有配置给用户关闭的设置」；`docs/settings-roadmap.md` §2.1 早已点名 `sync.capture_paused` 是「存储中已有但无 UI 的键」）：后台监听（特权直读 / 日志感知 / 悬浮窗轮询）此前只能从常驻通知的「暂停捕获」动作关停——OEM 通知栏折叠动作按钮时等于无处可关。现在设置页给出常驻开关，与通知动作同落 `sync.capture_paused`，两个入口互通：开启即停自动读取本机复制（后台读取后端经既有捕获门一并停转，常驻通知如实陈述「自动捕获已暂停」），分享面板/磁贴的手动发送、已记录内容的外发与接收照常；「暂停同步」则维持原语义不动。2 新键 × 19 语逐键齐全（`check-i18n-parity.py` 照绿）；ViewModel 持久化、捕获门重查与通知动作互通由 3 条新单测钉死。
- [Windows] 暂停同步全局快捷键（`docs/settings-roadmap.md` P1#9 的另一半，`53f9a2e`）：偏好 · 运行新增第二条快捷键行（`hotkey_pause`，默认关）——在任意应用按下组合键（需含 Ctrl/Alt/Win）即暂停同步，再按一次恢复，与托盘浮窗的暂停按钮同一意图落 `is_paused`（托盘颜色、浮窗、通路页捕获段随属性联动）。复用既有快捷键基座：`FlyoutHotkeyManager` 泛化为 `GlobalHotkeyManager`（两条快捷键同住一个隐藏消息窗口）、录入框按组合即设 / Backspace 清除、被其他程序占用时如实报「已被占用」（赭色事实行）；与呼出浮窗快捷键撞同一组合时呼出浮窗优先，暂停行如实报「已用于本应用的另一条快捷键」而非诬赖其他程序。4 新键 × 19 语逐键齐全（`check-i18n-parity.py` 照绿）。

## [0.1.0] - 2026-08-26

首个版本。分支：`main`（自 Stage 0–3 基线 `768fd1c` 以来；早期变更曾在 `cursor/implement-charter-ui-1991` 上推进，该分支已全部并入 main，后续变更直接落在 main）。

### 新增

- [Android] 首次打开引导教程扩为五步（2026-08-26 用户需求「第一次打开应用引导用户设置的教程，主题也要和应用一致」；扩展既有单页首跑引导，不另起炉灶）：欢迎（品牌 + 三席说明 + 「先说清楚」诚实盒，原页原样保留为第一步）→ 与 Windows 配对（复用配对仪式自己的文案键 `pairing_title`/`pairing_intro`，三条事实：入口固定在通路网络段、指纹一致才确认、配对完即启动服务）→ 打通后台读取（复用向导三路线键与代价文案，特权直读置顶挂「推荐」小签 + ●●● 质量点阵，附无线调试提示：开发者选项 → 无线调试后电脑经 Wi-Fi 同样一键启动，免 USB 线）→ 权限一览（通知/悬浮窗/电池优化，先说用途、到点才问、拒绝不碍前台复制）→ 就绪收尾（实心「去配对」直达扫码、着色「去通路页看路线引导」、幽灵「先看看」）。宪章主题贯穿：`ClipSyncColors` 日/夜双套、超椭圆控件、单屏唯一实心主角、进度点阵 + 「上一步」返回（系统返回键同义）、每步右上「稍后设置」永不困人；`Crossfade` 走宪章缓动、减弱动效时硬切。首跑判定沿用 `FirstRunStore`（已配对安装静默标记已读不打扰）；偏好页新增「帮助 · 重新查看引导」再入口，重看不改任何设置与配对。19 语文案逐键齐备（`check-i18n-parity.py` 照绿），`OnboardingContent` 结构由测试钉死（步序、路线顺位与推荐唯一性、权限清单、跳过出口），Android 全套 691 测试照绿 + ktlint/detekt 通过（detekt 基线随签名变更重生成）。
- [Windows] 首次打开引导教程（对齐 Android 五步教程，同一 `FirstRunStore` 语义）：首次手动启动弹出五步宪章主题向导——欢迎（品牌 + 三席说明 + 「先说清楚」诚实盒，Windows 视角改述：后台读取受限的是手机端，特权直读正是这台电脑帮手机启动）→ 配对（**真实配对二维码**，与配对窗同一 `PairingService` 一次性票据：进入配对步才签发、离开即作废、到期自动换新、DPI 整像素栅格；旁附电脑名 + 证书指纹核对卡；引导期间手机扫码完成即换成流动蓝「已配对」事实条）→ 特权直读（USB 数据线 / 无线调试 Android 11+ 两条路概览 + adb 只在点击时运行的同意说明，指向引导结束后的通路页特权直读卡）→ 蓝牙备援（可选一提，现在无需决定）→ 就绪收尾（实心「前往通路页」聚焦通路 tab、幽灵「开始使用」）。自绘宪章 chrome、日/夜主题经 DynamicResource 即时跟随、进度点阵 + 「上一步」、每步右上「稍后设置」永不困人——走完、跳过、关窗任一出口都标记已读，绝不再纠缠。首跑判定持久化于 settings 表（`onboarding_seen`）：已配对的安装（数据目录幸存于重装）静默标记已读不打扰；`--minimized` 自启不弹窗也不消费首开资格。偏好页新增「帮助 · 重新查看引导」再入口，重看不改任何设置与配对。28 新键 × 19 语逐键齐全（11 键逐字复用 Android 引导译文，parity 守门照过）；`FirstRunStore` 与步序结构由 8 条新单测钉死，Windows 全套 591 测试照绿、Release 0 警告。
- [Windows] 特权直读 adb 协助（`docs/settings-roadmap.md` 通路页扩展；无静默 adb，全程显式同意）：配对成功后，通路页新增「特权直读」卡片，帮用户把内置特权宿主拉起来——Android 安全模型不允许应用自己开这条通道，只能从电脑侧执行一次启动命令。
  - **显式同意闸门**：首次使用前必须先勾选授权（说明 adb 权限含义、`privileged_adb_consent` 持久化），未同意绝不调用 adb；随时可「撤销 adb 授权」。
  - **自动探测 adb + 手机**：`AdbLocator` 依次在随包 platform-tools、`ANDROID_HOME`/SDK 常见路径、`PATH` 中查找 `adb.exe`（不静默下载安装），卡片如实显示 adb 位置或「未找到」；`PrivilegedHostAssistant` 解析 `adb devices -l`，区分就绪/未授权/离线/无设备。
  - **一键「启动特权直读」**：手机已连且授权后，一键执行内置特权宿主 start.sh；配对完成时若已授予 adb 同意会自动探测手机。宿主未运行时提供再次启动入口（重启后特权通道需重来）。
  - **首次 USB 调试 + adb RSA 指纹弹窗仍需在手机上手动确认**——文档明确说明，不代劳。
- [Windows] 特权直读无线配对（Android 11+ 无线调试，无需数据线；与 USB 同一 `privileged_adb_consent` 显式同意闸门，绝不静默）：特权直读卡新增「无线配对（无需数据线）」折叠区（FoldToggle 把手，缺省折叠——USB 仍是主路），两条路任选：
  - **扫码配对（Android Studio 同款）**：出示 `WIFI:T:ADB` 配对二维码（随机服务名 + 一次性密码；密码只存在于二维码像素与 `adb pair` 参数，不落日志不上屏），手机在「无线调试 → 使用二维码配对设备」扫码后，电脑经 `adb mdns services` 发现手机的 `_adb-tls-pairing` 通告（按二维码的服务名精确匹配，绝不误抓 Android Studio 等并行会话）并自动 `adb pair` → 发现 `_adb-tls-connect` 端口 → `adb connect` 一条龙。出示期间每两秒查询一次 mDNS、限时两分钟、随时「停止出示」——轮询行为原样写在等待文案里，不背着用户跑。adb 无 mDNS（旧 platform-tools）时如实拒绝并指向配对码路。二维码复用配对窗的 DPI 整像素栅格配方（模块永不重采样），静区纯白。
  - **配对码配对**：Android Studio 式 `adb pair host:port code`——填手机配对弹窗的 IP:端口与六位配对码；配对成功自动查找连接端口，发现不了时留手动「连接 IP:端口」入口（连接端口与配对端口不同这一事实写进文案与排障表）。
  - 连接成功即汇回既有 USB 路径：自动「检测手机」，随后一键「启动特权直读」（无线设备 serial 即 `IP:端口`）。失败语汇诚实：配对被拒/过期给「重新打开手机配对入口」的赭色下一步提示；`adb connect` 文本裁决先于退出码（部分 adb 失败也退 0，绝不误报成功）；撤销 adb 同意/折叠面板/停止出示都会立刻掐掉二维码等待。
  - Core 层新增 `WirelessAdbEndpoint`（host:port 解析，IPv6 加括号、裸 IPv6 拒绝不猜）、`AdbPairingQrPayload`（转义 + round-trip 解析、密码学随机）、`AdbMdnsServicesParser`、`AdbPairOutcome`/`AdbConnectOutcome`、`WirelessPairingFlow` 状态机（转移表纯函数，陈旧异步完成拽不动 UI）与 `PrivilegedHostAssistant` 无线方法；94 条新单测覆盖命令逐 token 构建、QR 载荷、mDNS 解析、输出判读与状态机全表。29 新键 × 19 语逐键齐全（parity 守门照过）。`docs/install.md` §8.1 增 8.1.1 无线小节与排障三行。
- [Android] 特权直读路线卡接上「复制启动命令」（`CapabilityRoutes`）：特权通道未就绪时不再只显示状态，而是把内置特权宿主的确切 adb 启动命令一键复制到剪贴板（`PrivilegedHostStarter.adbCommand()`），并提示去哪执行（已连手机的电脑，或 Windows 端一键）。
- [双端] 移除 Shizuku 品牌标识，统一为「特权直读 / 内置特权宿主」（不改功能，仅标识）：用户可见诊断错误码 `SHIZUKU_*` → `PRIV_HOST_*`；能力上报授权名 `shizuku_*` → `priv_host_*`；Android/Windows UI 文案、安装与验证文档、设计宪章与预览稿一律改称特权直读。内部枚举 `SHIZUKU_EVENT`（持久化于偏好键，改名会丢用户已验证状态）、真实第三方库符号（`dev.rikka.shizuku`）、`THIRD_PARTY_NOTICES` 法务署名与竞品事实性引用按原样保留并加注说明。
- [Windows] 基础设置第一批（`docs/settings-roadmap.md` 的 Windows 半边，P0-1/P0-3/P0-5 + P1-7/P1-9/P1-15；外观手动覆盖当时暂缓，同日翻案后已落地——见下方外观条目）：
  - **历史字号**（偏好 · 显示，`ui_history_font_scale`，小/标准/大 = 0.9/1.0/1.15）：只缩放剪贴内容文字——历史预览正文、详情窗正文、托盘浮窗正文；组头、注记盒、元信息与按钮的纲领类型阶不动。历史区字号收敛为 `DynamicResource`，改动即全窗生效。
  - **预览行数**（偏好 · 显示，`ui_preview_lines`，2/4/6 行，默认 4）：历史列表每条的正文行数上限；行高显式化后截断永远落在整行边界，托盘浮窗保持两行封顶。
  - **开机自启 · 静默入托盘**（偏好 · 运行，`launch_at_startup`，默认关）：开关写入/删除当前用户 `HKCU\…\CurrentVersion\Run` 项（不用计划任务、不提权），自启带 `--minimized` 参数只落托盘；启动时重申注册表项，程序挪位后自愈。相应地，**手动启动现在直接打开主窗口**（此前一律纯托盘启动），自启路径保持安静。
  - **呼出浮窗快捷键**（偏好 · 运行，`hotkey_flyout`，默认关）：`RegisterHotKey` 全局组合键（需含 Ctrl/Alt/Win），在任意应用按下即呼出托盘浮窗；被其他程序占用时如实报「已被占用」（赭色事实行），输入框内按组合即设、Backspace 清除。
  - **清空历史**（偏好 · 数据 + 历史页，两步确认）：一次性删除本机全部条目（含图片 blob），确认文案明示「建议先导出」与本地删除语义（不远程撤回）；完成后在数据组陈述清空条数。
  - **保留条数可调**（偏好 · 存留，`retention_max_entries`，100–2000 步进 100，默认 2000）：一直存在的 2000 条清理上限从硬编码接入 settings 表与两处清理调用，超出上限时最旧条目先过期。
- [Android] 基础设置第一批（`docs/settings-roadmap.md` 的 Android 半边，P0-1/P0-4/P0-5 + P1-7/P1-8/P1-15；存储键与视图模型已先行落地，本批补齐偏好页 UI 与接线）：偏好页按路线图 §4.1 重组为 显示 · 同步 · 捕获 · 历史 · 运行 · 数据 · 设备 七组，每组超椭圆卡片 + 分组头：
  - **历史字号**（偏好 · 显示，`ui.history_font_scale`，小/标准/大 = 0.9/1.0/1.15，就地分段选择）：只缩放历史列表预览正文（在 sp 之上叠乘，系统字号继续叠加），组头、注记盒、元信息与按钮的宪章类型阶不动。
  - **预览行数**（偏好 · 显示，`ui.preview_lines`，2/4/6 行，默认 4）：历史列表每条正文的行数上限，即改即生效。
  - **跳过敏感内容**（偏好 · 捕获，`capture.skip_sensitive`，默认开）：开关落 UI，描述如实说明依赖来源应用的敏感标记、分享面板主动发送不受限。
  - **收到内容通知**（偏好 · 运行，`notify.inbox`，默认开）：应用内总开关；关闭只静默通知面，同步与历史照常。关闭时通路页以灰面事实条如实陈述后果（「收到内容通知已关闭」，指路偏好 · 运行），与系统级「通知已关闭」横幅同一形制、二者只出现其一。
  - **保留时长 / 保留条数**（偏好 · 历史，−/+ 步进行，与 Windows 步进器同形制）：保留时长 1–3650 天随「自动过期清理」联动置灰（关闭时值槽如实显示「永久保留」），保留条数 100–10000 步进 100 一直生效。
  - **清空历史**（偏好 · 数据，两步就地确认）：首点换成灰面确认条（本机删除、不可撤销、建议先导出），确认后一次删除全部条目含图片 blob，完成后在数据组陈述清空条数。
- [双端] 外观手动覆盖与多语言的共享底座（`docs/settings-roadmap.md` P1#6 / P1#16——2026-08-25 用户裁决翻案：#6 此前「主动跳过」为误、多语言从「明确不做」改列必做）：偏好键双端落地——`ui.theme` / `ui_theme`（`system`｜`day`｜`night`，默认跟随系统）与 `ui.language` / `ui_language`（`system` 或 BCP-47 标签，默认跟随系统），无法解读的存值一律回落「跟随系统」不报错；19 种语言目录（简体中文、繁體中文、English、日本語、한국어、Español、Français、Deutsch、Português (Brasil)、Русский、العربية、Italiano、Tiếng Việt、ไทย、Bahasa Indonesia、हिन्दी、Türkçe、Polski、Nederlands——各以母语名展示，永不翻译；仅阿拉伯语为 RTL）在 Android（`i18n.LanguageCatalog`）与 Windows（`Ui/LanguageCatalog.cs`）逐条对齐，双端测试钉死防漂移。主题/语言的 UI 开关、接线与全量文案提取待后续批次（RTL 布局策略见路线图 P1#16 注记）。
- [双端] 设计宪章 UI 全面落地：令牌系统、日/夜主题运行时切换、随包三字族（Noto Sans SC / Plus Jakarta Sans / JetBrains Mono）、超椭圆卡片与动效令牌、polyline 应用图标（Windows ICO + Android 自适应图标）、全中文文案。
- [Android] 完整同步栈：Room 存储（序号分配、接收向量、outbox 同事务）、pinned-TLS WebSocket 同步引擎、指数退避重连、`connectedDevice` 前台服务。
- [Android] 三个无权限入口（分享面板、快捷磁贴、通知「复制」动作）与前台自动捕获上行。
- [Android] 入站收件箱 + `auto_apply_remote` 自动写回 + 失败回退通知；回环抑制。
- [Android] 三档后台读取后端代码落地：特权直读事件（内置特权宿主 UserService + `IClipboard` 反射适配）、ADB 日志 + 悬浮窗（AOSP/OneUI/MIUI-HyperOS/ColorOS 四族版本化解析器）、悬浮窗轮询（焦点控制器 + 不可触摸不变量）。**真机验证已于 2026-08-26 经用户签核完成（`docs/manual-qa-results.md` 签核节为凭据）；逐设备明细待回填前，矩阵槽位按「未测不得改绿」字面纪律维持 `NOT_TESTED`（见 `docs/device-validation-matrix.md`），不构成逐 ROM 兼容性承诺。**
- [Android] 通路页能力向导与真实探针（读/写能力分离探测）、对端可达性周期重探、首次运行引导、全套空状态、`POST_NOTIFICATIONS` 引导与关闭状态行。
- [Android] 开机恢复链（默认关，含诚实恢复通知）、保留期清理、用户可调大小上限。
- [Windows] 托盘四态图标 + 440px 托盘浮窗（最近剪贴 + 暂停开关）、托盘诊断查看器 + 认证锁定通知。
- [Windows] 自绘宪章标题栏、配对 QR/确认窗口重皮肤、通路页接真实会话状态（连接数、发件队列、对端确认至）。
- [双端] 暂停/私密模式在捕获、队列、引擎逐层真实关断，恢复后补投无丢失。
- [双端] 蓝牙备援传输（ADR 0005，默认双端关闭；阶段 0 实体机 spike 已判定 GO——见「文档 / 测试」；产品路径随 2026-08-26 用户签核视为已验证（明细待回填），更多适配器/OEM 的矩阵化覆盖仍以 `docs/bluetooth-fallback-plan.md` 阶段 5 为准）：IP 全部不可达时，已配对设备可经蓝牙 RFCOMM 继续同步文本（协议 v1 运行于 bt1 安全信道内，图片不过蓝牙）。Android：`BluetoothSyncConnector` RFCOMM 拨号 + `Bt1ClientHandshake`/`Bt1SyncTransport`，`SyncSupervisor` 只在所有 IP 候选失败后拨蓝牙（证书 pin 不符绝不降级）、蓝牙会话内持续探测 IP 并自动回切；偏好页开关 + bonded 设备选择器 + `BLUETOOTH_CONNECT` 授权引导，通知与通路页显示「蓝牙备援」。Windows：`ClipSync.Peer.Bluetooth` 双 TFM 程序集（可移植 bt1 监听栈 + WinRT `RfcommServiceProvider` 监听端），`BluetoothSyncHost` 单会话接受循环复用 `SyncSessionEngine` 与 `AuthThrottle`；偏好「蓝牙备援」开关 + 通路页网络段状态行（待命/同步中/适配器不可用）。双端单测经内存流覆盖握手正反例、帧层攻击负例与端到端双向同步；安装文档新增蓝牙配对指引，威胁模型新增近场攻击面条目。
- [双端] 蓝牙备援传输阶段 1——bt1 握手与帧层（纯逻辑，无平台蓝牙依赖）：`docs/protocol-bt1.md` 定稿安全信道协议（共享 `pair_secret` 的 HMAC-SHA-256 双向认证、HKDF-SHA-256 按方向派生 AES-256-GCM 会话密钥、4 字节大端长度前缀 + 计数器 nonce 帧、7 MiB 明文上限、`BT1_` 错误码）；`protocol/bt1/` 新增跨语言测试向量与消息 fixtures 并纳入 `scripts/validate-protocol.py` 校验；C#（`ClipSync.Core/Security/Bt1`）与 Kotlin（Android `sync` 包）双端实现，针对同一 fixtures 的单测含篡改/重放/乱序/截断/超限负例。尚无任何真实蓝牙 I/O；RFCOMM 传输、降级编排与 UI 均属后续阶段（见 `docs/bluetooth-fallback-plan.md`）。
- [分发] 最小分发链（阶段 7 裁剪版）：`scripts/package-windows.ps1` 产出自包含 win-x64 便携 ZIP（含运行时/许可/安装指南 + SHA-256，Linux CI 经 `EnableWindowsTargeting` 可产包）；`scripts/package-android.ps1` 产出 Release APK（签名只读 `CLIPSYNC_ANDROID_*` 环境变量，密钥库不入库，另有 Debug/未签名校验路径）；`docs/install.md` 一页中文安装/配对/通路/排障指南（并随 Windows ZIP 分发）。

- [双端] 减弱动效跟随系统（`docs/settings-roadmap.md` P1#13，无应用内开关——系统的选择是事实）：系统开启「减弱动态效果/移除动画」时，2.6 s 赭色「需要你操作」脉动改为静态 act 描边。Android 观察 `ANIMATOR_DURATION_SCALE`（改设置即时生效，无需重启），同时定格通路管道的流动点、tab 切换 Crossfade 改硬切；Windows 以 `SystemParameters.ClientAreaAnimation` 门控通路捕获段脉动光环，静态描边独自承担提醒。
- [双端] 设备色手动改（`docs/settings-roadmap.md` P1#14，`ui-gap-audit.md` P2 项收口）：通路设备行新增五色点选色器，可把某台设备的邻近色固定为 dev-1..dev-5 中任意一档；点配对顺位默认色即恢复「跟随配对顺位」（存储保持最小——只存覆盖）。颜色属设备身份：撤销/重新配对不清除；历史与浮窗的来源注记盒随生效色。Android 存 `PairingStore`（`device.accent.<id>`），Windows 存 devices 表 `accent_override` 列（schema v4→v5 纯增列迁移）。
- [Windows] 通路「网络段 · 已配对设备」新增残留设备检测与一键清理：同名同平台的旧档（同一部手机换了身份重新配对留下的幽灵）与超过 14 天未连接的设备以赭色标记（标注各自积压的待发条数），列表上方横幅给出总数与总积压并提供「一键清理」——撤销全部残留、作废其密钥并清空其发件队列，「待发」计数即时回落。
- [双端] 外观手动覆盖（`docs/settings-roadmap.md` P1#6，2026-08-25 用户裁决翻案后落地；底座 `ea97333`、双端接线 `aa91a34`）：偏好 · 显示新增「外观」三选一——跟随系统（默认）/ 日间 / 夜间，只在既有的日/夜两套纲领配色之间选择，绝无取色器（纲领裁决：色板不是用户变量）。改动即生效：Windows 换整本令牌字典（所有开着的窗口经 DynamicResource 即时重排），Android 在 `ClipSyncTheme` 组合前解析覆盖值并按生效主题重申 edge-to-edge——手动固定日间时即使系统处于深色，状态栏/导航栏图标对比也跟随生效主题而非系统。跟随系统时继续监听系统切换；手动固定时忽略系统广播直到切回。存储键 Android `ui.theme` / Windows `ui_theme`（`system`/`day`/`night`，无法解读的存值回落跟随系统）。已知边界（按设计保留，行内文案如实陈述）：Windows 托盘图标活在任务栏里，始终跟随任务栏深浅采样，手动覆盖只作用于窗口 chrome。
- [双端] 多语言（`docs/settings-roadmap.md` P1#16，2026-08-25 用户裁决翻案后落地；底座 `ea97333`）：偏好 · 显示新增「语言」选择——跟随系统（默认）+ 19 种语言（唯一权威目录表见路线图 §五：简体中文、繁體中文、English、日本語、한국어、Español、Français、Deutsch、Português (Brasil)、Русский、العربية、Italiano、Tiếng Việt、ไทย、Bahasa Indonesia、हिन्दी、Türkçe、Polski、Nederlands），每种语言恒以母语名展示、永不翻译。**回落规则双端一致且文档化**：无法解读的 `ui.language`/`ui_language` 存值回落「跟随系统」；缺译键回落简体中文（Android 缺省资源 = zh-Hans，Windows 中立资源 = zh-Hans）——当前两端全部语言逐键齐全，回落面为空。Android：359 键 UI 文案全量抽入 string resources，ViewModel 与纯构建函数持 `UiText`（Res/Raw）延迟到 UI 层解析，`AppCompatDelegate.setApplicationLocales` 在 Application onCreate（任何 UI 前）重放存值，**改语言即时生效**（`4c89766`；18 语翻译 `b2398aa`/`af84683`/`85aed94`/`77c12c7`；Robolectric 资源限定符钉死 zh-rCN `d7c941c`）。Windows：225 键文案以 `strings.json` 为唯一事实源生成 resx（`scripts/generate-windows-strings.py`），XAML 经 `x:Static` 编译期校验取键，`LocalizationManager` 在任何窗口前把 `ui_language` 应用为进程 UI 文化，**更换后重启生效**（行内文案如实陈述；`8a4bc32`，卫星语言五批 `b6bd2d1`/`f76b5c7`/`8e1dc2e`/`22e6a63`/`4eb0fb0`），非中文界面品牌词显示拉丁「ClipSync」（与 Android 同约定）。阿拉伯语 RTL：Android Compose 布局随 per-app locale 整体自动镜像，仅通路管道流线与配对取景框两处装饰性 Canvas 保持 LTR（方向无语义、不载文字）；Windows 每窗按 `FlowDirection` 镜像，指纹/监听地址等机器文本钉回 LTR。回退范围已在路线图 P1#16 注记如实注明。
- [双端] 历史「仅本机保留」标注（ADR 0005 §5 落地，收口 `docs/bluetooth-product-path-qa.md` 记录的缺口）：仅文本会话（蓝牙备援窗口，或对端图片开关关闭的 v1 路由）内被 `local_only` 终止的本机图片，从对端游标越过那一刻起在**本端**历史打上中性灰「仅本机保留」注记盒（Windows 历史列表徽章行 / Android 图片卡「图片」徽章旁）——事实陈述不是警报，不用错误红。标记随事件持久化（Windows clips 表 `local_only_at` 列，schema v5→v6；Android Room `local_only_at` 列，v2→v3，均纯增列迁移），重启不丢；首次降级时间为准，重连不刷新。若之后 v2 会话把同一事件以可用头重新公告或整体送达（如中断重播），过时标记自动清除。文本条目永不带此标注。Windows 主窗开着时标注即时可见：会话引擎在标记真正落库/清除时发 `LocalOnlyMarksChanged`（IP 监听与蓝牙备援两条宿主链都接到主窗历史刷新），不用等下一次捕获或远端提交；Android 由 Room 失效自动推给历史流，本就即时。

- [双端] 配对成功的一次性镜面流光（tokens §9 唯一豁免「仅配对成功可考虑一次」落地，ui-gap-audit 动效令牌行收口；配方按 technique_lab §05）：一条容器宽 36% 的白色渐变带，倾斜 18°，以宪章缓动 `cubic-bezier(.16,1,.3,1)` 从 -40% 平移到 130% 只走一遍，绝不循环——停顿比划过本身更重要。Android 在配对完成卡入场时扫过（1.3s，`drawWithContent` 渐变刷，卡面超椭圆裁切）；Windows 在配对二维码窗收到 `PairingCompleted` 时扫过整窗（0.9s + 0.35s 静止一拍）再关窗，替代原先的立即关窗。两端均随系统「减弱动效」让路：Android `LocalReducedMotion`、Windows `ClientAreaAnimation`，关闭动画时行为与从前逐位一致（立即关窗/无流光）。首次连接场景（technique_lab 亦允许）刻意未接：连接是反复事件，宪章的一次性豁免只给配对仪式。
- [双端] 日夜切换交叉淡化（tokens §9 `dur-theme` 400–450ms 落地，动效令牌最后一项待接线收口）：外观在日/夜之间切换（手动覆盖或跟随系统翻转）时不再硬切。Android 对整套宪章调色板按通道插值（面阶、文字阶、状态色、设备邻近色与颗粒强度同滑），450ms 宪章缓动；Windows 在换令牌字典的同一帧把每个开着的窗口冻结成旧调色板快照盖在上层，再以 400ms `KeySpline(.16,1,.3,1)` 淡出——新调色板从快照下透出，屏幕上永远不出现硬切（托盘浮窗刻意除外：无时间性动效是其既有裁决）。两端「减弱动效」时保持硬切，应用启动首次应用主题不播过渡。
- [Windows] 会话内帧级速率限流（stage-6 硬化 W5 收口）：`FrameRateBudget` 固定窗（默认 10 000 帧/分钟，16 MiB 图片分块流仍有宽裕）在 JSON 解析与重放哈希之前计数——已认证对端的紧循环帧洪泛不再能白烧本端 CPU；超限发可重试 `RATE_LIMITED` 后关会话，对端按既有退避重连并经 `want_ranges` 续传无丢失。
- [Android] 收件箱通知洪泛上限：失控或恶意对端连推大量剪贴时，通知面每 30 秒窗口只放行前 5 条逐条通知，其余合并为一张就地更新的计数卡（「收到多条内容 · 另有 N 条」）——不再刷满通知栏，也不再任由 Android 系统限流静默丢弃；录入与自动写入永不受限，仅通知形态变化，计数卡沿用无正文契约。

### 变更

- [发布] Android APK 版本号与发布 tag 强制对齐（发布准备）：`release.yml` 把 tag 版本（去 `v` 前缀）经 `package-android.ps1 -Version` 注入 Gradle（`-PversionName`/`-PversionCode`，`build.gradle.kts` 新增属性覆盖支持）——versionName 与 tag 逐字一致，versionCode 按 `major×1000000 + minor×10000 + patch×100 + 尾数`（rc 取序号、正式版取 99）推导，rc → 正式版 → 后续版本升级顺序单调递增；带 tag 的发布产物从此不可能再打进 `build.gradle.kts` 里过期的硬编码版本。tag 形态不是 `vX.Y.Z`/`vX.Y.Z-rc.N` 时打包即失败；本地打包不带 `-Version` 沿用开发默认值；未配置签名 secrets 时的未签名兜底路径不受影响（同样带版本注入）。Windows ZIP 的 tag 注版（`package-windows.ps1 -Version`）为既有行为，未变。
- [双端] 图标与诊断窗的两处令牌偏差归位（strict-audit §2.2 P3 收口）：应用图标底渐变底 stop 从 `#D6E0EC` 归正为 tokens `bg-grad` 的 `#DAE3EE`（中停位 0.45→0.42 同步对齐；SVG 源、Android 自适应图标背景、重生成的 Windows `app.ico` 三处一致）；Windows 诊断窗日志区改用随包 JetBrains Mono（机器的声音，pack URI 直引、不引主题字典保持自包含），Consolas 退为加载失败兜底。
- [Android] 能力路线去品牌化：Shizuku 在 UI 中呈现为「特权直读」。
- [Android] 收件箱文本解析迁至 Room（硬化项收口）：通知「复制」动作按事件 ID 直查 Room 历史行，收到的文本不再在 SharedPreferences 里留第二份明文（旧 50 条残留一次性清除）；删除历史即令复制动作失效（诚实语义——数据真的没了就不该还能复制出来）。
- [Windows] 三窗口全部令牌刷 `DynamicResource` 化以支持运行时换肤。
- [双端] IA 迁移（`docs/settings-roadmap.md` P1 #10–12）：连通性配置从偏好迁回通路网络段——它们改变「内容能不能到达对端」，按判据属通路。**蓝牙备援**（双端；Android 为网络段下方的备援卡片，含目标设备选择与 bonded 设备清单）、**额外监听地址**（Windows，「重启后生效」赭色说明照搬）、**本机证书指纹**（Windows，偏好「信任」卡随之取消）现住在通路页「网络段 · 连接」卡。设置键与行为不变，纯搬家；偏好页各留一个发布版本的链接行（「已移至通路 · 网络」）指路，下个版本删除。`docs/install.md` 的 Tailscale 与蓝牙备援路径说明同步更新。
- [Windows] 页面切换接动效令牌（ui-gap-audit P2 收口）：历史/通路/偏好三页切换由硬切改为 200ms 宪章缓动淡入（`cubic-bezier(.16,1,.3,1)`，对齐 Android tab Crossfade）；沿用 `ClientAreaAnimation` 门控，系统减弱动效时保持硬切。随后详情窗开窗接入同一语汇（`PageFadeIn` 族上移 App 级共享；托盘浮窗刻意不接——无时间性动效是其既有裁决）。
- [Windows] 计数文案去「(s)」括号硬凑（与 Android 复数化同一轮 i18n 语法收口）：残留设备横幅、LAN 已连接状态行、导出完成、通路网络/设备两段计数、法语托盘状态行等 7 键在 en/de/fr/es/it/nl/pt-BR/pl/ar 诸语改标签式计数（「Devices connected: N」形制，俄语/阿语译文原本就如此规避）——resx 无复数选择机制，标签式是不引入复数引擎的正解，西语/意语「Se detectaron 1 dispositivos」这类单数错配随之消失。
- [双端] 开关读屏文案补全（ui-gap-audit P3 可访问性）：Windows 全部 9 个 CharterToggle 与配对 QR 图补 `AutomationProperties.Name`（引既有标题串，i18n 零负担）；Android 偏好开关行与蓝牙备援卡改整行 `toggleable(Role.Switch)`——TalkBack 一站读出标题+描述+状态、触达区扩到整行，裸 Switch 转纯显示。
- [Android] 计数文案复数化（i18n 语法收口）：7 个含计数的键（通知洪泛正文、向导剩余步数、预览行数、保留天数、保留条数、导出完成、清空完成）从 `<string>` 转 `<plurals>`，19 语按各自 CLDR 复数域补齐分支（俄语/波兰语全变格、阿拉伯语六分支含双数、西法意葡补百万级 many 构式）——英语「1 step(s) left」这类括号硬凑从此消失；`check-i18n-parity.py` 同步扩展复数键逐键与占位符校验。
- [Android] Android 12+ 备份/迁移姿态显式化：manifest 补 `dataExtractionRules` 与 `fullBackupContent="false"`（云备份与设备间迁移全域排除，与 `allowBackup=false` 同一裁决——剪贴历史是敏感明文、Keystore 密钥本就不可迁移，半截恢复只会伪装已配对）。
- [双端] 历史列表信息架构归位（2026-08-26 用户反馈：图片本身才是内容主角，元数据不得担任标题）：图片行的 `image/png 32×32 · 96 B` 从最大号正文降为缩略图下方/侧方的安静灰单色注记盒三枚（编码 / 尺寸 / 字节数，tokens §6 机器的声音；不可知的项直接缺省不显示「?」），缩略图即内容。Windows 文本行左侧的空灰占位盒取消——改为按 ADR 0003 渲染期格式的类型提示线条图标（链接/邮箱/验证码/凭据各有形，普通文本用文字页图标），描边随格式邻近色；文本预览保持正文主导。Android 图片卡为缩略图补齐同一套元数据注记盒（Room 历史流 LEFT JOIN blob 索引带出 mime/尺寸/字节，索引缺失时注记盒整体隐藏）；文本卡本就无占位盒无需改动。「图片」「仅本机保留」徽章、设备邻近色、行高与字号档位全部不动。
- [双端] 同类问题巡检（同一反模式：技术元数据置于人类内容之上）：Windows 详情窗——图片可解码时元数据挪到时间行下的单色注记行（`image/png · 320×200 · 2 KiB`），正文不再以元数据充当内容、不可解码时正文如实「无预览」；Windows 托盘浮窗——图片条目补 36px 缩略图 + 「图片」正文，不再以元数据串充当预览。配对、通知、通路/健康页两端巡检未见同类问题（Android 通知无正文是既有隐私契约）。历史字号默认档双端核实本就是标准 1.0，无需回调。
- [Windows] ScrollViewer 滚动条宪章化：全应用隐式 `ScrollBar` 样式落 `CharterControls.xaml`——透明轨道、去箭头按钮、10px 圆角拇指用 `CsLine2Brush`（悬停 `CsText4Brush`、拖动 `CsText3Brush`，全部 DynamicResource 随日/夜主题即时换色），替换系统默认的通用灰。Android LazyColumn 本就不渲染系统滚动条，无需处理。

### 修复

- [双端] 通路「对端写入」不再永远「未探测」（人工 QA 2026-08-25 缺陷 3）：Windows `/v1/peer/health` 新增 `clipboard_apply_text` 自报字段（`off`/`paused`/`unverified`/`applied`/`failed`——off/paused 是对端用户的姿态设置，其余是本会话最近一次真实剪贴板写入的证据，绝不「API 存在即就绪」）；Android 健康探测解析该字段并映射到对端写入段：已验证/已开启 → 就绪，对端关闭自动写入、对端已暂停、写入失败 → 降级并注明「依据：对端在 /v1/peer/health 的自报」。旧版对端或字段缺失时仍显示诚实的「未探测 · 对端未上报」而非负面猜测。IP 同步正常时顶栏不再停留「通路部分接通」。
- [双端] 超限文本（>1 MiB）拒绝不再静默（人工 QA 2026-08-25 缺陷 4）：Windows 主窗历史页显示事实条「刚复制的文本超过 1 MiB：内容保留在系统剪贴板，未截断，但不记录历史、不同步」（「知道了」可关闭，下一次成功捕获自动退场），主窗隐藏在托盘时补发气泡通知；Android 自动捕获同场景弹出 Toast「复制的文本超过 1 MiB：仅保留在本机剪贴板，不同步（未截断）」。提示只陈述尺寸事实，绝不含剪贴内容；暂停/私密/去重等预期内的拒绝保持安静。
- [Windows] 启动时不再遗留空控制台窗口：当进程被控制台方式拉起（如 `dotnet ClipSync.App.dll`，窗口标题为 dotnet.exe 路径）时，应用在 `OnStartup` 里检测并分离继承来的控制台（`GetConsoleWindow` + `FreeConsole`），保持纯托盘启动；应用运行时自身不派生任何子进程。
- [Android] 通知栏不再回落成系统默认图标（绿色机器人）：小图标去掉主题属性着色（SystemUI 跨进程解析不了主题属性时会整体回退），三个通知渠道归入统一「剪贴同步」渠道组，前台服务通知补齐宪章配色（polyline 图标 + 流动蓝 #215F8F）、低优先级与无时间戳。
- [Windows] 历史列表图片缩略图不再显示为空灰块：修复并发刷新下缩略图临时文件互相踢掉导致的静默失败（改为每次尝试独立临时名，输者复用赢者成品）；WIC 解码/编码故障（含 COMException）降级而非中断刷新；位图在 `FromEntry` 一次解码并冻结后绑定，容器回收不再重解码；确实无法出图时显示诚实的「无预览」占位而非空灰块。
- [Windows] 历史列表追加三处修复：(1) 缩略图管线自愈——缓存缩略图文件损坏（旧版本遗留、磁盘故障）时绑定即删除并从 blob 重建，重建仍失败则直接按 128px 有界解码原始 blob 兜底，「无预览」占位从此只在 blob 缺失或真不可解码时出现（新增诊断码 `thumbnail_cache_regenerated` / `thumbnail_blob_bind_fallback`），占位字形与文字由 text-4 提升为 text-3 保证可辨；(2) 来源应用无法识别时不再显示裸英文「Unknown source」——改为与设备/形态注记盒同形制的中性灰注记盒「未知来源」（accent 0 灰族令牌：`CsSurface3`/`CsLine2`/`CsText3`），元信息行只留时间，详情窗同步显示「来源：未知来源」；(3) 条目布局跨类型统一——所有条目共享固定 56px 左槽（图片=缩略图或「无预览」，文本=安静的三行文字线字形占位），右侧标题、注记盒、元信息行纵向对齐并整体居中，图片行与文本行不再左缘参差（链接/账号/验证码/密码/图片形态注记全部保留）。
- [Windows] `scripts/build-windows.ps1` 在真实 Windows 上不再因两条缩略图单测失败而 exit 1（2026-08-25 人工 QA 阻断项）：(1) 缩略图像素断言从逐通道全等改为 ±3 容差 + alpha≥250——`DecodePixelWidth` 走 WIC Fant 缩放器，其定点权重在纯色区域也可能带来每通道 1–2 的偏差，全等断言在真机上过严（网住的回归——透明/空白像素——仍远超容差）；(2) 损坏缓存自愈不再依赖能否删除旧文件：新增强制重建路径（唯一临时名 + 覆盖式 `File.Move`）绕过 `Ensure` 的 exists/length 门，旧「先删再 Ensure」在删除被暂时锁定（杀软/索引器）时会把损坏文件原样返回、缓存永不自愈；重建后仍不可解码时 `LoadForList` 返回空路径而非把不可解码的缓存路径交给详情窗绑定。
- [Windows] 位图文件加载（`BitmapFile.TryLoad`）在真实 Windows 上从未成功过，现已修复（首次 Windows CI 运行 32827123288 暴露，6 条 `ClipSync.App.Tests` 失败中的 5 条同源）：`CreateOptions` 里的 `IgnoreImageCache` 与流式加载（无 `UriSource`）组合时，WPF `BitmapImage.FinalizeCreation` 会调用 `ImagingCache.RemoveFromImageCache(null)` 抛 `ArgumentNullException`，而 `TryLoad` 把它当解码失败吞掉——于是每次调用都静默返回 null：历史列表一直靠 `BitmapDecoder` 兜底解码原始 blob 才有图（缩略图缓存路径从不被绑定），详情窗大图则直接落空。WPF 的图片缓存本就只作用于 URI 加载的位图，该旗标对流式加载纯属无效负担，予以移除（`IgnoreColorProfile` 保留）。同一批 CI 失败中的第 6 条是独立小缺陷：`HistoryDisplayOptions.StoredScaleFor` 用 `double.ToString` 生成存值，「标准」档写成 `"1"` 而非路线图键契约的 `"1.0"`，现改为字面拼写（读取侧 `double.TryParse` 两种拼写都认，旧存值透明迁移）。诊断过程：Linux 无法执行 WPF 测试体，靠一条临时的不吞异常诊断测试在 windows-latest 上取到真实异常栈后定位（已随修复移除）。
- [双端] 开启系统代理（Clash/Surge 等）时同步不再被劫持或断连：Android 端 OkHttp（同步、配对、健康探测）显式 `Proxy.NO_PROXY` 直连，Windows 端 `ClientWebSocket.Options.Proxy = null` 直连；TUN/VPN/全局模式的放行方法见 `docs/install.md` 第 5 节。
- [Windows] 合并 tray-diagnostics 后的 `DiagnosticsWindow` 构建错误。
- [Android] 快捷磁贴不再永远亮着（ui-gap-audit P2 诚实性收口）：磁贴现随会话状态实时变化——仅当到对端的会话真正在线（点按立即送达）时才是 ACTIVE，暂停、私密模式、连接中、等待重连、未配对一律 INACTIVE 但保持可点（点按仍入队或给出诚实 Toast）；纯映射逻辑抽出为 `SendClipboardTileState` 单测钉死。
- [Windows] 托盘图标随系统主题热切换（ui-gap-audit P2）：任务栏深浅此前只在启动时采样一次，深色启动后切到浅色任务栏会留下低对比图标；现监听 `SystemEvents.UserPreferenceChanged` 重采样并整套换图标，四态语义不变。
- [Windows] 配对 QR 高 DPI 整数缩放（ui-gap-audit P3）：`pixelsPerModule` 从固定 8 改为按显示器 DPI 与目标边长计算，模块严格对齐物理像素（DPI 变更事件即时重渲染，`NearestNeighbor` 兜底）——高分屏上二维码不再因分数像素缩放发糊。
- [Android] 状态栏/导航栏背景色从 styles.xml 硬编码抽入 `@color/cs_bg`（日/夜两套，ui-gap-audit P3）：与 tokens.md 的 bg 令牌单一事实源对齐。
- [Android] 自动写入通知配色与 Compose 弃用告警。
- [Android] 远端图片自动写入改为独立开关「自动写入远端图片」（`auto_apply_images`，默认关）：文本「自动写入剪贴板」不再连带把图片写进本机剪贴板，与 Windows 端及 ADR 0004（图片写入门独立于文本自动应用）对齐；暂停同步仍同时关断两者，图片照常进入历史可手动复制。
- [Android] 收到远端图片不再静默落库（plan 5.6「收到的内容绝不无声着陆」诚实性收口）：此前图片自动写入未开启（默认态！）或写入失败时**什么通知都不发**，与文本「进收件箱并通知」的行为相悖——远端发来的图片就这样悄悄躺进历史。现在补上无正文的「来自电脑的新图片」到达卡（19 语齐全）：不带缩略图、不带哈希、也刻意不带「复制」动作（文本收件箱解析不了图片），点开进应用看历史缩略图；自动写入成功时照旧发既有的「已自动写入」状态卡。到达卡与文本卡共用同一洪泛闸与「收到内容通知」总开关，无正文契约不变。
- [Windows] 同一部手机重新配对不再积累幽灵设备（2026-08-25 人工 QA 缺陷 2：设备表出现两台同名 `Xiaomi 22041216C`，未撤销的旧档 `80d29726-…` outbox 积压 42 条 pending，把通路「待发」带高）：配对确认时若列表已有同名同平台的活跃设备，视为同一部手机换了身份（如清除应用数据后）重新配对——确认窗以赭色提示「将替换旧记录」，批准后旧记录在同一事务内自动撤销（作废配对密钥、trust epoch +1）并清空其 outbox 积压，其在线会话被立即断开；设备撤销连带清空该设备发件队列为既有行为，本次补充测试锁定。
- [双端] 图片同步默认值双端对齐为**默认关**（2026-08-25 人工 QA 限制 5「双端图片同步开关不一致（Win 开 / Android 关）」）：产品默认按 ADR 0004 与设计宪章 §5.9（`settings_image_sync_hint`「默认关闭」）统一为**双端关闭、显式开启**——Android `SyncSettingsStore.imageSyncEnabled`（`sync.image_sync`）与 Windows 偏好页 `image_sync` 设置本就默认关（QA 机上 Windows 为「开」系该机历史上手动开启后的持久化状态，非出厂默认）；真正修掉的是 Windows 端两处**未接线即放行**的库层默认：`SyncSessionOptions.ImageSyncEnabled` 与 `PeerSyncHost` 的兜底闸原为 `() => true`（任何忘记接线的宿主会静默参与 image_clip_v2 收发图片，与双端「默认关」相悖），现改为 fail-closed `() => false`——未接线的宿主表现为纯 v1 文本对端（拒绝 `/v2` 升级、图片一律 `unsupported_media`/`local_only`）；测试套件在 `PeerPair.DefaultSessionOptions` 显式开启以继续覆盖 v2 图片路径，新增 Windows `ImageSyncGateTests.ImageSyncGateDefaultsOffMatchingTheAndroidDefault` 与 Android `ImageSyncDefaultAlignmentTest` 双向钉死默认值。

### 文档 / 测试

- `THIRD_PARTY_NOTICES.md` 依赖清单对齐现实（2026-08-26；该文件随 Windows 便携 ZIP 分发，Stage 3 之后一直未更新）：Shizuku API（`dev.rikka.shizuku:api`/`provider` 13.1.5，特权直读的 Binder 桥，库名按上游法务署名保留）、Room 2.6.1、WorkManager 2.10.0 从「计划中、待核证」移入「当前已声明」表并补齐实核许可证（Shizuku-API 经 pinned artifact POM 与上游 LICENSE 核证为 MIT）；补录 AppCompat、KSP/detekt/ktlint 构建插件、Robolectric 与 AndroidX Test（test only）、CI 的 Python 协议校验工具；移除已不存在的 JsonSchema.Net 行（协议校验早已改走 Python `jsonschema`）。纯清单更正，不改任何依赖。
- [Android] 会话级图片集成测试补齐（关闭 `docs/verification-without-device.md` 声明的已知缺口）：新增 `sync/WindowsAndroidImageSyncChainTest`——脚本化 v2 Windows 监听端驱动真 Room + 真内容寻址 blob 存储上的 `SyncEngine` 图片 chunk 状态机，覆盖 announce → want_ranges → clip_fetch → begin/chunk/end → ack 双向字节精确往返（共享 media fixture 与其公示的 41+42 分块）、同 hash 二次公告免传直落库、v1 会话 `local_only` 降级 + 仅本机保留 badge 落 Room。
- [双端] **蓝牙备援阶段 0 spike 实测完成，判定 GO**（2026-08-25，报告 `docs/bluetooth-phase0-report.md` 含双端完整日志）：Lenovo/Realtek Windows 25H2 × Redmi Note 11T Pro（Android 13/MIUI）bt1 模式 256 KiB 档连续 3 轮全通——未打包进程 WinRT `RfcommServiceProvider` 发布 SDP 成立（G-W1，无需 MSIX）、Android 授权流/bonded 枚举/建连成立（G-A1）、bt1 端到端成立（G-C1）、建连 0.7–2.2 s（G-P1 ≤5 s）、RTT 中位约 31 ms（G-P2 ≤500）、吞吐 150–180 KiB/s（G-P3 ≥50）、稳定性 3/3（G-S1）。随之更新：`docs/bluetooth-fallback-plan.md` 阶段 0 → 已完成（含结果摘要与阶段 3/5 缺口清单）、ADR 0005 状态与限制表改用实测校准值、`docs/device-validation-matrix.md` 新增蓝牙 spike 证据行、`docs/install.md`/spike 运行手册补充小米实测配对路径（Windows 扫不到手机时从手机「可用设备」发起）。**纠正 `docs/install.md` 第 7 节错误陈述**：蓝牙窗口内复制的图片「恢复 IP 后按序补传」为误——按 ADR 0005 §4，图片事件在蓝牙会话中以 `local_only` 终止标记推进游标，**事后不会补传**（历史中标注「仅本机保留」），现与 ADR/应用内文案一致。仍无任何 READY 声明：spike 走工具路径，应用内蓝牙备援的整机验证属阶段 5。
- [双端] 蓝牙备援阶段 0 可行性 spike 材料（仅证据收集，非产品功能，无 READY 声明）：运行手册 `docs/bluetooth-phase0-spike.md`（前置条件与 OS bonding/ClipSync 配对的区别、双端逐步操作、期望输出样例、GO/REVISE 门槛、排障表）与空白报告模板 `docs/bluetooth-phase0-report-template.md`；Windows 监听端 spike 控制台工具 `scripts/spike-bt1-windows/`（+ 包装脚本 `scripts/spike-bt1-windows.ps1`）——未打包进程经 WinRT `RfcommServiceProvider` 发布冻结服务 UUID、只收一个连接、复用 `ClipSync.Core` 阶段 1 bt1 实现做真实握手，独立于 `ClipSync.sln` 且经 `EnableWindowsTargeting` 可在 Linux 编译（`TreatWarningsAsErrors` 生效）；Android 客户端 spike 为 debug 构建独有的「ClipSync BT Spike」入口（`android/app/src/debug/`，声明 `BLUETOOTH_CONNECT` 仅入 debug manifest，release APK 无蓝牙权限与 spike 代码）——枚举 bonded 设备、`createRfcommSocketToServiceRecord` 连接、bt1 握手、RTT/上下行吞吐测量，结果以 `SPIKE_RESULT:` 结构化行输出（logcat 标签 `ClipSyncSpike`）便于本地代理采集；`docs/bluetooth-fallback-plan.md` 阶段 0 小节同步链接上述材料。
- CI 工作流重构为三作业：协议 schema/fixture 校验、Windows 构建 + 全部测试、Android 单元测试 + debug APK 组装；在 `cursor/**` / `feature/**` 分支与 PR 上运行。2026-08-25 扩为五作业：新增 Android 静态分析（`detekt` + `ktlintCheck`，随基线守新违规）与 i18n 齐全性（`scripts/check-i18n-parity.py` 校验双端 19 语逐键齐全 + 占位符一致，另以生成器重跑 + `git diff --exit-code` 把守 Windows resx 与 `strings.json` 同步）。同日再扩第六作业 `e2e-stage4`：`scripts/run-e2e-stage4.ps1` 上 CI（ubuntu-latest）——真 Kestrel + TLS pin + WebSocket 的 `ClipSync.E2eHost` 监听端由 Android JVM 拨号套件（`com.clipsync.android.e2e.*`）驱动，双向各恰一次收敛才 `E2E-PASS`；入 CI 前先在 ubuntu 本地实跑通过验证（脚本对非 Windows 宿主经 `EnableWindowsTargeting` 编译，本就跨平台）。
- 跨端 E2E 补图片段（protocol v2 首次跨语言实跑）：此前 E2E 只收敛文本（v1 拨号），图片链路的最强证据止于「脚本化假扮对端」（Android `WindowsAndroidImageSyncChainTest`）与 Windows 自环集成——两个产品实现从未在真实 TLS 会话里互传过图片。现 `scripts/run-e2e-stage4.ps1` 在 Windows 侧播种共享 media fixture `png-8x8`，新增 `e2e/CrossClientImageSyncE2eTest`（Room 真库 + 真内容寻址 blob 存储的 Kotlin `SyncEngine` 拨 `/v2`）把 `jpeg-1x1` 推回：入站 begin/chunk/end 重组后逐字节比对、出站等到对端 ack、脚本终检 Windows 库两枚 hash 各恰一枚（故意用不同 fixture，防 v2 hash 去重掩盖单向断链）。图片段以独立配对身份 `android_image_device_id` 拨号——每个拨号测试都从 seq 1 起步的新库出发，共用身份会让两段在 Windows 侧的 (origin, seq) 键空间相撞（首次本地实跑抓到的真实冲突：图片段先跑污染了文本段的 seq 1，文本段末步断言失败）；`ClipSync.E2eHost` 配对两台 Android 设备并按 ADR 0004 显式开启图片同步（产品默认关不变）。扩展后脚本在 ubuntu 本地实跑 `E2E-PASS` 后才入 CI。
- [Windows] 远端自动写入决策抽为纯函数并补齐测试（`WindowsAndroidSyncChainTest` 的 Windows 侧镜像缺口）：`App.OnRemoteClipsCommitted` 内嵌在 Dispatcher 回调里的「暂停仍收但不写 / 批次仅最新一条 / 文本与图片双闸独立 / 图片闸关绝不回落旧文本 / 无 hash 图片不写」策略此前无任何直接测试；现抽出 `RemoteApplyDecision.Decide(batch, isPaused, autoApplyRemote, autoApplyImages)` 纯函数（App 装配层只执行其裁决，行为逐位不变），`RemoteApplyDecisionTests` 七个用例与 Android 链路测试逐场景对齐。
- 发布工作流 `.github/workflows/release.yml`（2026-08-25）：推送 `v*` tag 即打包 Windows 便携 ZIP（版本号取自 tag）与 Android APK（`CLIPSYNC_ANDROID_*` secrets 配置齐全时签名，否则如实产出 unsigned 并在发布说明明示不可安装），连同 `.sha256` 附到 GitHub Release；发布说明自动注明「发布 ≠ 真机验证通过」的诚实边界。尚未打过任何 tag，首次实跑待第一个发布。
- CI/发布工作流的 actions 依赖升到当前大版本（2026-08-25，清 Node 20 弃用与 setup-java v4 停更警告）：`checkout` v4→v7、`setup-java` v4→v6、`setup-dotnet` v4→v6、`setup-python` v5→v7、`gradle/actions/setup-gradle` v4→v6、`upload-artifact` v4→v7、`download-artifact` v4→v8——各家大版本跳变均为 Node 运行时/凭据文件类基础设施变化，本仓工作流用到的输入面不变；ci.yml 六作业随本次推送实跑验证，release.yml 首个 tag 实跑时验证。
- 测试规模（截至 main `73b0afb`，六作业 CI 全绿；前一快照 `d573080` 时点为 652 + 484 + 186）：674 Android JVM（常规跑含 2 条跨端 E2E 拨号用例跳过——其实跑归 `e2e-stage4` 作业）+ 489 跨平台核心/对端 + 199 Windows 应用层用例；含 Windows↔Android 全链路脚本化集成测试（文本与图片链各一）与真实会话事件驱动的通路页验证。
- 新增 `docs/verification-without-device.md`（绿测 ≠ 兼容的边界）、`docs/stage-gap-audit.md`、`docs/competitive-analysis.md`、`docs/design/ui-gap-audit.md`、`docs/manual-qa-checklist.md`、`docs/release-notes-template.md`；扩充 `docs/device-validation-matrix.md` 为脚本化检查清单。
- 新增 `docs/releases/v0.1.0-draft.md`：首个发布的发布说明骨架（按模板预填亮点/变更明细/安全/已知限制的叙述；构建 SHA、产物校验和、CI 链接、人工 QA 计数保留占位符，发布时替换）。骨架不改变发布门槛：首轮人工 QA 判定「不能出 RC」当时仍然有效（该门槛其后由 2026-08-26 用户签核解除，见「已知欠账」实体机验证条）。**2026-08-26 定稿为 `docs/releases/v0.1.0.md`**：占位符按实回填或如实注明待回填，产物文件名对齐打包脚本实跑（`ClipSync-windows-x64.zip` / `ClipSync-android.apk`）。
- [Android] lintDebug 告警清理（2026-08-25，16 错误 + 46 告警 → 0 错误 + 22 告警）：错误全数清零（RestrictedApi/PrivateApi 系按 shizuku 包路径豁免——特权直读的存在意义就是反射隐藏 API；蓝牙拨号与磁贴旧分支定点 `@SuppressLint` 附因由）；告警侧收掉 ObsoleteSdkInt（minSdk 29 恒真检查与 `-v26` 目录限定）、AutoboxingStateCreation、德语「die die」Typos 误报、SetWorldReadable（脚本供 adb shell/root 异 uid 消费属设计）与 7 处 PluralsCandidate（见「变更」复数化条目）；detekt/ktlint 对 Composable 命名启用规则层豁免，告别基线行号漂移雷区。剩余 22 条均为记录在案的刻意选择（依赖版本通知 19 条 + ApplySharedPref/ModifierParameter/BatteryLife 各 1）。

### 已知欠账（进行中）

- 发布产物上传：发布 CI 已落地（`release.yml`，tag `v*` 触发，见「文档 / 测试」），但本节归档（2026-08-26）时尚未打过任何 tag——Releases 页仍无产物。tag 路径已定：先 `v0.1.0-rc.1`（含连字符的 tag 由 `release.yml` 自动标记 prerelease，用于验证发布工作流首次实跑与产物完整性），确认无误后打 `v0.1.0` 正式发布。商店/F-Droid 上架未做。历史导出/导入已落地（Windows `5fd7461`、Android `3c51350`、图片感知 v2 格式 `87c0016`），自本清单移除。
- ~~会话内 WebSocket 帧级速率限流仍未做（预认证 per-IP 滑窗限流已落地，`25d2788`）；收件箱 Room 化、入站通知洪泛策略未做。~~ 三项已全部收口（2026-08-25，见「新增/变更」）：帧级限流 `793c0b9`、收件箱 Room 化 `f928537`、通知洪泛上限 `17fda6e`。
- 实体机验证：2026-08-25 首轮人工 QA（`docs/manual-qa-results.md`）判定「不能出 RC」；**2026-08-26 用户签核真机验证已全部完成，RC 门槛已过**（签核小节见同文件）。逐设备结构化明细（矩阵 S0–S4 计数、P95 样本）待用户提供后回填，`docs/device-validation-matrix.md` 槽位单元格在回填前按字面纪律维持 `NOT_TESTED`。发布收尾进度：本节归入 `[0.1.0]` 与发布说明定稿（`docs/releases/v0.1.0.md`）已于 2026-08-26 完成，剩打 tag（先 `v0.1.0-rc.1` 后 `v0.1.0`）触发 `release.yml`。
