# ClipSync v0.3.0（发布说明 · 定稿）

> **状态：已发布（2026-09-02）；tag `v0.3.0` 已推送，`release.yml` 已实跑产包并创建 Release。**
> APK 的 versionName/versionCode 由 tag 注入（`v0.3.0` → versionName `0.3.0`、
> versionCode 30099；推导规则见 `docs/install.md` §10 / `scripts/package-android.ps1`）。
> 「下载与校验」表的 SHA-256 与签名指纹已在 tag 实跑后回填并就已发布产物复核；GitHub Release
> 正文以工作流自动生成的产物表为准。
> **发布结果**：`v0.3.0` 已于 2026-09-02 发布成功（`release.yml` run
> [33614825008](https://github.com/fraternidaddeng/clipsync/actions/runs/33614825008)），
> 产出已签名 APK 与 Windows 便携 ZIP 各带 `.sha256`。`/releases/latest` 现指向本版。

---

# ClipSync v0.3.0

- 发布日期：2026-09-02
- 构建 commit：`e769051`（tag `v0.3.0` 指向；代码基线为 main `fcc41a3`——其后至定稿只有 CHANGELOG
  定版与本说明，不含任何代码变更）
- 分支/标签：`v0.3.0`（自 `main`）

## 下载与校验

产物文件名与打包脚本实产一致（`scripts/package-windows.ps1` / `scripts/package-android.ps1`，
不带版本号后缀；版本号由 tag 注入程序集与 APK versionName/versionCode，并打进 Release 页标题）：

| 产物 | 文件 | SHA-256 |
|---|---|---|
| Windows 便携版 | `ClipSync-windows-x64.zip` | `2e81f06f93cb61cf7241c0f572ca46f12ba467c65558659b4c097203140c2c1f` |
| Android APK | `ClipSync-android.apk` | `f7de7e36d68dea19ed43f0969a78d9e6262d1a381ca062b3b1b8e2f3b8e9d79b` |

（tag 实跑后回填的实测值，与 Release 正文自动注明及产物旁 `.sha256` 一致；APK 的哈希已就已发布
产物本体下载复核。构建 SHA：`e7690510e11be922e1faed1f8abc73e8ae3cecc7`。）

> `CLIPSYNC_ANDROID_*` 签名 secrets 已配置，本版 Android 产物为**已签名**的 `ClipSync-android.apk`
> （未配置时会退化为不可安装的 `ClipSync-android-unsigned.apk`，Release 正文会如实注明；
> 本版 Release 正文确认走的是已签名路径，资产列表中无 unsigned APK）。

APK 签名指纹（证书 SHA-256，跨版本必须一致）：

```
47:1D:EF:D5:11:E7:7B:21:92:6D:9C:73:D5:64:B5:D9:3E:E6:FA:B7:E9:5F:DD:BB:EB:BE:A8:05:96:03:40:84
```

证书主体 `CN=DENGLEI`。已解析本版已发布 APK 的 v2 签名块，证书 SHA-256 与 `v0.2.0` /
`v0.1.0-rc.2` 相同——旧正式包可直接覆盖升级到本版。注意：该 CI 分发密钥与仓库外本地开发用的
`clipsync-release.keystore`（指纹 `1A:D0:2D:…`）并非同一把；分发产物一律以 CI secrets 注入的
上述密钥为准。

## 本版亮点

**这一版加上了应用内更新。** 偏好新增「关于」组，显示本机正在运行的版本号，并对照 GitHub 仓库
`fraternidaddeng/clipsync` 的最新正式 Release（`/releases/latest`，不含预发布）决定要不要升。
检查不会偷偷下载；发现新版本后由你点「下载并安装」。文件落地后先核 SHA-256（优先用 Release
资产的 `digest`，没有再拉 `.sha256` 侧车），对不上就丢掉、不安装。

Android 把已校验 APK 交给系统安装器（需允许本应用安装未知来源）；Windows 便携目录在应用退出后
由助手脚本覆盖再重启——用户数据在 LocalAppData，不会被碰到。

**谁该升级：还在用 `v0.2.0` 或更早版本、希望以后不用每次去网页下包的人。** 本版是这条通路的
起点：`v0.2.0` 里没有检查器，**升到 0.3.0 必须从本页手动下载一次**；装上之后，再出的正式
发布就可以在应用里完成。

## 变更明细

逐条流水见 `CHANGELOG.md` 的 `[0.3.0]` 节。以下为归并摘要。

### 新增

- [双端] 偏好 · 关于：当前版本、检查更新、发现新版本后的「下载并安装」。
- [双端] 对照 GitHub `/releases/latest` 的版本比较（`0.1.0-rc.2` < `0.1.0` < `0.2.0` < `0.3.0`）。
- [双端] 下载后强制 SHA-256 校验；缺 digest 且无侧车则拒绝安装。
- [Android] 已校验 APK 走系统安装器；未授予「安装未知应用」时先打开系统设置。
- [Windows] 便携 ZIP 解包后写助手脚本，退出进程再 `robocopy` 覆盖并重启。
- 19 语文案逐键齐全。

### 变更

- 无（本版不改同步协议、配对或默认开关）。

### 修复

- 无（本版是新能力，不是缺陷修复轮）。

### 安全

- 更新通道只拉本仓库正式 Release 的具名资产（`ClipSync-android.apk` /
  `ClipSync-windows-x64.zip`），不把 `ClipSync-android-unsigned.apk` 当安装包。
- 哈希对不上即删除文件、不调用安装器 / 不覆盖便携目录。
- 无静默后台下载；检查与安装都是用户点的。

## 验证状态（诚实声明，不得省略）

- 自动化：本版新增的更新单元测试已在本地跑绿（Windows Core Update 28、Windows App Update
  相关、Android `com.clipsync.android.update.*` + 偏好检查更新例）；`check-i18n-parity.py`
  绿（Android 419 键 / Windows 329 键 × 19 语）；`detekt` + `ktlintCheck` 绿。完整 Windows /
  Android / e2e-stage4 / secret-scan 以 tag 推送后的 CI 与 `release.yml` 为准。
- 人工 QA：本版**未重跑**完整 `docs/manual-qa-checklist.md`，也未在真机上走一遍「检查 →
  下载 → 安装」闭环。沿用 2026-08-26 用户签核结论（`docs/manual-qa-results.md`）。
- 实体机矩阵：`docs/device-validation-matrix.md` 槽位 D1–D4 仍全部 `NOT_TESTED`。
  **矩阵槽位回填前不构成逐 ROM 兼容性承诺。**

## 已知限制

- **从 `v0.2.0` 升到本版不能走应用内更新**——旧版没有检查器。请从本 Release 手动下载。
- 应用内更新只认正式 Release（`/releases/latest`），不会提示预发布（`*-rc.N`）。
- 本版的下载/安装路径未做真机端到端签核；首次使用请仍核对本页 SHA-256。
- 特权直读、后台图片、蓝牙仅文本、Windows 未代码签名等既有边界见 `docs/releases/v0.2.0.md`
  「已知限制」，本版未改变它们。

## 升级说明

- **Android**：与 `v0.2.0` 同签名密钥时可直接覆盖安装（versionCode `20099` → `30099`），
  **配对与历史不丢**。debug 签名包与正式包互不兼容——换签名必须先卸载，卸载会删除设备上的
  历史与配对。
- **Windows**：解压覆盖即可，`%LOCALAPPDATA%\ClipSync` 数据目录不受影响。回滚 = 换回旧的
  解压目录。本版起，之后的正式发布可在偏好 · 关于里下载并替换该便携目录。
- **无需重新配对**：本版未改配对协议、密钥格式与协议版本协商。
- 全新安装、配对、授权与排障见 `docs/install.md`（Windows 便携 ZIP 内亦附带）。安装前请核对
  Release 正文注明的 SHA-256（与产物旁 `.sha256` 文件一致）。
